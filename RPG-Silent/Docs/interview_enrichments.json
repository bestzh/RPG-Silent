{
  "1": {
    "answer": "- **结论**：数据小、不可变、按值传递且无身份语义时用 struct；有生命周期、需多态或共享引用时用 class。大 struct 频繁拷贝的成本可能超过引用类型的 GC 开销，需按实际 profiling 验证。\n- **机制**：struct 复制值，class 复制引用——理解这一点才能解释「为什么」而非只背结论。\n- **考察点**：值类型、引用类型、GC，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- **项目落地**：DamageInfo 可用 struct，Controller 用 class。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "struct 复制值，class 复制引用。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "DamageInfo 可用 struct，Controller 用 class。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 为了避 GC 滥用大 struct——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「struct 和 class 在 Unity 中如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 值类型、引用类型、GC 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "2": {
    "answer": "- **结论**：delegate 是委托类型定义；event 是带访问限制的委托，外部只能订阅/退订不能触发；Action 和 Func 是内置的泛型委托，分别用于无返回值和有返回值场景。\n- **机制**：event 只暴露 add/remove——理解这一点才能解释「为什么」而非只背结论。\n- **考察点**：回调、封装、事件模型，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：UI、战斗、任务通知。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "event 只暴露 add/remove。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "UI、战斗、任务通知。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 订阅不解绑——对象销毁后仍收到回调 → MissingReferenceException 或逻辑重复执行\n- 只背「C# 委托、事件、Action、Func 有什么区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 回调、封装、事件模型 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "3": {
    "answer": "- **结论**：async/await 可改善异步流程可读性，但在 Unity 中必须处理：回到主线程操作引擎 API、异常传播（避免 async void）、以及对象销毁后的取消/判空。推荐绑定 CancellationToken 或使用 UniTask。\n- **机制**：await continuation 依赖上下文——理解这一点才能解释「为什么」而非只背结论。\n- **考察点**：异步、主线程、取消，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- **项目落地**：登录、资源加载、HTTP。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "await continuation 依赖上下文。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "登录、资源加载、HTTP。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- async void 滥用——异常无法被调用方捕获，Unity 控制台只报 UnobservedTaskException\n- 只背「async/await 在 Unity 中有哪些坑」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 异步、主线程、取消 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "4": {
    "answer": "- **结论**：通过 hash 定位桶，冲突后比较 key。 扩容会重排 entry。\n- **考察点**：哈希表、扩容、冲突，面试时应先给出判断标准再展开。\n- 内部 `Entry[]` + 桶数组，负载因子超阈值时扩容为约 2 倍并重算 hash 索引。\n- 自定义 key 的 `GetHashCode()` 必须稳定：CombatEntity 作 key 时若 hash 随状态变会导致查不到。\n- `List<T>` 默认容量 4，翻倍扩容会 `Array.Copy` 并产生 GC；`new List<T>(64)` 预设容量。\n- **项目落地**：配置表、实体映射。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "扩容会重排 entry。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "配置表、实体映射。 实现时可基于 `async/await` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 可变对象作 key——导致运行时异常或性能回退\n- 只背「Dictionary 的底层原理是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 哈希表、扩容、冲突 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "5": {
    "answer": "- **结论**：yield 生成状态机，Unity 协程驱动 IEnumerator。 MoveNext 恢复执行位置。\n- **考察点**：迭代器、协程，面试时应先给出判断标准再展开。\n- 编译器为 iterator 生成状态机类，每次 `MoveNext()` 从上次 `yield` 处恢复。\n- `StartCoroutine` 返回 `Coroutine` 句柄，`StopCoroutine` 或 `OnDisable` 必须停止，否则对象销毁后仍执行。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：剧情、技能流程。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "MoveNext 恢复执行位置。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "剧情、技能流程。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 把协程当线程——导致运行时异常或性能回退\n- 只背「IEnumerable、IEnumerator 和 yield return 的关系是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 迭代器、协程 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "6": {
    "answer": "- **结论**：new、字符串、闭包、装箱、集合扩容都会产生 GC。 对象释放依赖 GC 扫描。\n- **考察点**：托管堆、临时分配，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- `List<T>` 默认容量 4，翻倍扩容会 `Array.Copy` 并产生 GC；`new List<T>(64)` 预设容量。\n- **项目落地**：对象池、缓存集合。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "对象释放依赖 GC 扫描。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "对象池、缓存集合。 实现时可基于 `GC.Alloc` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Update 中 LINQ——迭代器 + 委托分配，战斗 Update 每帧触发 GC Spike\n- 只背「C# GC 在 Unity 中如何产生」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 托管堆、临时分配 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "7": {
    "answer": "- **结论**：捕获局部变量会生成闭包对象。 编译器生成 display class。\n- **考察点**：Lambda、捕获变量，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- **项目落地**：UI 回调、排序委托。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "编译器生成 display class。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "UI 回调、排序委托。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 循环变量捕获错误——导致运行时异常或性能回退\n- 只背「闭包为什么可能产生 GC」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Lambda、捕获变量 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "8": {
    "answer": "- **结论**：值类型转 object 或接口可能装箱。 值复制到堆对象。\n- **考察点**：装箱拆箱、接口、object，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- **项目落地**：日志、非泛型集合。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "值复制到堆对象。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "日志、非泛型集合。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- struct 频繁进 ArrayList——导致运行时异常或性能回退\n- 只背「boxing/unboxing 是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 装箱拆箱、接口、object 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "9": {
    "answer": "- **结论**：容量不足会申请更大数组并复制。 扩容成本随元素数增长。\n- **考察点**：容量、数组复制、GC，面试时应先给出判断标准再展开。\n- `List<T>` 默认容量 4，翻倍扩容会 `Array.Copy` 并产生 GC；`new List<T>(64)` 预设容量。\n- 热路径用 `Clear()` 复用而非 `new List`，注意 `Count=0` 后 Capacity 仍保留。\n- **项目落地**：战斗临时列表预设容量。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "扩容成本随元素数增长。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "战斗临时列表预设容量。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每帧 new List——导致运行时异常或性能回退\n- 只背「List 扩容机制会带来什么问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 容量、数组复制、GC 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "10": {
    "answer": "- **结论**：少量拼接用 string，大量循环拼接用 StringBuilder。 string 拼接会生成新对象。\n- **考察点**：不可变字符串、拼接，面试时应先给出判断标准再展开。\n- 每次 `+` 拼接生成新 immutable string；循环内用 `StringBuilder` 或 `ZString`（UTF16 零 GC 方案）。\n- `$\"HP:{hp}\"` 插值在 Unity 仍会分配，热路径改用 `StringBuilder.AppendFormat` 或缓存格式串。\n- **项目落地**：日志、聊天、UI 文本。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "string 拼接会生成新对象。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "日志、聊天、UI 文本。 实现时可基于 `async/await` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Update 中字符串拼接——导致运行时异常或性能回退\n- 只背「string 和 StringBuilder 如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 不可变字符串、拼接 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "11": {
    "answer": "- **结论**：ref 可读写，out 必须赋值，in 只读引用传递。 可避免大 struct 拷贝。\n- **考察点**：参数传递、拷贝控制，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- **项目落地**：数学计算、配置查询。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "可避免大 struct 拷贝。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "数学计算、配置查询。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 滥用 ref 降低可读性——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「ref、out、in 的区别是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 参数传递、拷贝控制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "12": {
    "answer": "- **结论**：表达不可变值，减少误修改。 readonly 成员避免防御性复制。\n- **考察点**：不可变、拷贝、防御性复制，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- **项目落地**：坐标、范围、ID 包装。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "readonly 成员避免防御性复制。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "坐标、范围、ID 包装。 实现时可基于 `GC.Alloc` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- mutable struct——导致运行时异常或性能回退\n- 只背「readonly struct 有什么价值」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 不可变、拷贝、防御性复制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "13": {
    "answer": "- **结论**：接口定义能力，抽象类复用状态和默认行为。 C# 单继承多接口。\n- **考察点**：多态、复用、契约，面试时应先给出判断标准再展开。\n- 结合 `Span<T>` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：IAttackable、BasePanel。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "C# 单继承多接口。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "IAttackable、BasePanel。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 接口过胖——导致运行时异常或性能回退\n- 只背「接口和抽象类如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 多态、复用、契约 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "14": {
    "answer": "- **结论**：泛型提供类型安全和复用，减少装箱。 编译期保留类型约束。\n- **考察点**：类型安全、复用、装箱，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：对象池、事件系统。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "编译期保留类型约束。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "对象池、事件系统。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- where 约束缺失——导致运行时异常或性能回退\n- 只背「泛型的优势和限制是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 类型安全、复用、装箱 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "15": {
    "answer": "- **结论**：out 支持返回协变，in 支持参数逆变。 保证类型安全方向。\n- **考察点**：泛型变体、类型转换，面试时应先给出判断标准再展开。\n- 结合 `IEnumerator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：事件处理器、只读集合。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "保证类型安全方向。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "事件处理器、只读集合。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 随意强转集合——导致运行时异常或性能回退\n- 只背「协变和逆变是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 泛型变体、类型转换 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "16": {
    "answer": "- **结论**：Unity Inspector 序列化的是字段（field），不是普通 C# 属性（property）；想让私有数据可配，用 `[SerializeField] private` 或 Unity 2020+ 的 `[field: SerializeField]`。\n- **机制**：Unity 内置序列化器只遍历标记了 `[SerializeField]` 的字段或 public 字段，不会走 property 的 get/set；`[NonSerialized]`、`[HideInInspector]` 可控制是否写入磁盘或是否显示。\n- **auto property 示例**：`[field: SerializeField] public float Speed { get; private set; }` 可以序列化 backing field，但 Inspector 里字段名不如显式 `_speed` 直观，团队协作时难搜索。\n- **推荐写法**：`[SerializeField] private float _moveSpeed;` + 只读属性 `public float MoveSpeed => _moveSpeed;`，既封装又可在 Inspector 调参。\n- **ScriptableObject/Prefab**：修改序列化字段会触发 YAML 脏标记；属性若只在运行时计算则不落盘，适合 `[SerializeField]` 存原始值、property 做校验/换算。\n- **Unity 应用**：配置组件、关卡参数、Addressables 引用都应走字段序列化；Editor 扩展用 `SerializedObject.FindProperty(\"_moveSpeed\")` 而非直接访问 property。",
    "principle": "Unity 序列化基于反射扫描字段元数据并写入 YAML/二进制资产，与 C# 编译器生成的 property 访问器是两套体系。property 的 getter/setter 逻辑在反序列化阶段不会执行，因此运行时依赖 setter 校验的数据可能在 Inspector 改值时绕过检查。",
    "project": "在 RPG-Silent 中，`PlayerSpawnPoint` 用 `[SerializeField] private DungeonDatabase dungeonDatabase` 暴露副本表引用，而 `alignRotation` 等开关同样走字段序列化；若改成 `{ get; set; }` 自动属性且不加 `[field: SerializeField]`，Prefab 上会丢失引用导致出生点回退逻辑失效。",
    "mistakes": "- 把业务数据写成 public property 却在 Inspector 找不到——策划无法配表，运行时全是默认值，表现为「改了参数不生效」。\n- 在 property setter 里做范围钳制，但 Inspector 直接写字段绕过 setter——出现负速度、超大碰撞半径等脏数据。\n- 混用 `[SerializeField]` 与 public 字段不统一——Review 时无法一眼看出哪些是可配设计数据。"
  },
  "17": {
    "answer": "- **结论**：static 生命周期跨对象，需处理重置。 编辑器域重载影响状态。\n- **考察点**：全局状态、域重载，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- **项目落地**：全局配置、服务定位。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "编辑器域重载影响状态。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "全局配置、服务定位。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 静态缓存场景对象——导致运行时异常或性能回退\n- 只背「static 在 Unity 中有什么风险」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 全局状态、域重载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "18": {
    "answer": "- **结论**：const 编译期替换，readonly 构造后不可改。 const 跨程序集有版本风险。\n- **考察点**：编译期常量、运行期常量，面试时应先给出判断标准再展开。\n- 结合 `GC.Alloc` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：配置默认值、运行时只读。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "const 跨程序集有版本风险。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "配置默认值、运行时只读。 实现时可基于 `GC.Alloc` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- public const 暴露版本问题——导致运行时异常或性能回退\n- 只背「const 和 readonly 有什么区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 编译期常量、运行期常量 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "19": {
    "answer": "- **结论**：using 保证 Dispose 调用。 非托管资源需确定性释放。\n- **考察点**：资源释放、句柄，面试时应先给出判断标准再展开。\n- 结合 `Span<T>` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：文件、网络、NativeArray。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "非托管资源需确定性释放。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "文件、网络、NativeArray。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只等 GC 释放——导致运行时异常或性能回退\n- 只背「using 和 IDisposable 的作用是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源释放、句柄 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "20": {
    "answer": "- **结论**：异常用于异常流程，不用于普通控制流。 抛异常成本高且影响栈。\n- **考察点**：try/catch、性能、稳定性，面试时应先给出判断标准再展开。\n- 结合 `NativeArray<T>` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：启动、加载、网络边界捕获。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "抛异常成本高且影响栈。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "启动、加载、网络边界捕获。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 空 catch 吞错——导致运行时异常或性能回退\n- 只背「异常处理在游戏热路径中如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 try/catch、性能、稳定性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "21": {
    "answer": "- **结论**：可读性好，但热路径可能产生分配和迭代成本。 委托和枚举器可能分配。\n- **考察点**：可读性、分配、性能，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- **项目落地**：编辑器工具可用，战斗慎用。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "委托和枚举器可能分配。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "编辑器工具可用，战斗慎用。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Update 中链式 LINQ——迭代器 + 委托分配，战斗 Update 每帧触发 GC Spike\n- 只背「LINQ 在 Unity 中的利弊是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 可读性、分配、性能 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "22": {
    "answer": "- **结论**：取决于 Unity 版本和运行时支持，适合无分配切片。 ref struct 不能装箱和跨 await。\n- **考察点**：内存切片、栈限制，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- **项目落地**：数据解析、临时缓冲。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "ref struct 不能装箱和跨 await。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "数据解析、临时缓冲。 实现时可基于 `async/await` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 在字段中保存 Span——导致运行时异常或性能回退\n- 只背「Span<T> 在 Unity 中能否使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 内存切片、栈限制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "23": {
    "answer": "- **结论**：lock 保护共享状态，但要控制粒度和顺序。 竞争会阻塞线程。\n- **考察点**：线程安全、死锁，面试时应先给出判断标准再展开。\n- 结合 `UniTask` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：日志队列、下载队列。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "竞争会阻塞线程。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "日志队列、下载队列。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 锁 Unity 对象——导致运行时异常或性能回退\n- 只背「多线程中 lock 的使用风险是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 线程安全、死锁 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "24": {
    "answer": "- **结论**：volatile 保证读写可见性，不保证复合操作原子。 防止部分重排序和缓存。\n- **考察点**：内存可见性，面试时应先给出判断标准再展开。\n- 结合 `GC.Alloc` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：后台线程停止标记。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "防止部分重排序和缓存。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "后台线程停止标记。 实现时可基于 `GC.Alloc` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用 volatile 做计数——导致运行时异常或性能回退\n- 只背「volatile 的作用是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 内存可见性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "25": {
    "answer": "- **结论**：Task 表示异步任务，Thread 是操作系统线程包装。 Task 通常复用线程池。\n- **考察点**：任务调度、线程池，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：后台解析、下载后处理。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "Task 通常复用线程池。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "后台解析、下载后处理。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 大量创建 Thread——导致运行时异常或性能回退\n- 只背「Task 和 Thread 有什么区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 任务调度、线程池 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "26": {
    "answer": "- **结论**：反射灵活但慢，IL2CPP 裁剪需保留类型。 AOT 下动态能力受限。\n- **考察点**：元数据、性能、IL2CPP，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：配置绑定、编辑器工具。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "AOT 下动态能力受限。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "配置绑定、编辑器工具。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 热路径反射——导致运行时异常或性能回退\n- 只背「反射在 Unity 中的成本和限制是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 元数据、性能、IL2CPP 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "27": {
    "answer": "- **结论**：Attribute 给类型或成员附加元信息。 反射或 Unity Inspector 消费。\n- **考察点**：元数据、编辑器扩展，面试时应先给出判断标准再展开。\n- Unity 序列化走字段反射，不执行 property setter；`[FormerlySerializedAs]` 可兼容字段重命名。\n- `[SerializeReference]` 支持多态引用序列化，适合技能/对话节点树。\n- **项目落地**：自定义校验、按钮、配置标记。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "反射或 Unity Inspector 消费。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "自定义校验、按钮、配置标记。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时频繁扫描——导致运行时异常或性能回退\n- 只背「Attribute 在项目中如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 元数据、编辑器扩展 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "28": {
    "answer": "- **结论**：新增字段给默认值，删除字段保留兼容期。 旧数据不含新字段。\n- **考察点**：字段新增、默认值，面试时应先给出判断标准再展开。\n- Unity 序列化走字段反射，不执行 property setter；`[FormerlySerializedAs]` 可兼容字段重命名。\n- `[SerializeReference]` 支持多态引用序列化，适合技能/对话节点树。\n- **项目落地**：存档、配置、协议缓存。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "旧数据不含新字段。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "存档、配置、协议缓存。 实现时可基于 `async/await` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 字段改名不迁移——导致运行时异常或性能回退\n- 只背「序列化 JSON 时如何处理版本兼容」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 字段新增、默认值 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "29": {
    "answer": "- **结论**：发布者持有订阅者委托会阻止释放。 委托引用目标对象。\n- **考察点**：引用链、订阅方生命周期，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：UI 页面订阅全局事件。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "委托引用目标对象。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "UI 页面订阅全局事件。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- OnEnable 订阅 OnDestroy 解绑——导致运行时异常或性能回退\n- 只背「C# 事件导致内存泄漏的原因是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 引用链、订阅方生命周期 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "30": {
    "answer": "- **结论**：将同一类型拆到多个文件，常用于生成代码。 编译后仍是一个类型。\n- **考察点**：代码生成、分文件，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- **项目落地**：UI 绑定生成、配置生成。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "编译后仍是一个类型。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "UI 绑定生成、配置生成。 实现时可基于 `GC.Alloc` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 手写和生成互相覆盖——导致运行时异常或性能回退\n- 只背「partial class 的适用场景是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 代码生成、分文件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "31": {
    "answer": "- **结论**：极端性能或原生交互才用 unsafe。 绕过托管安全检查。\n- **考察点**：指针、性能、平台，面试时应先给出判断标准再展开。\n- 结合 `Span<T>` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：图像处理、Native 插件。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "绕过托管安全检查。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "图像处理、Native 插件。 实现时可基于 `Span<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 未校验越界——导致运行时异常或性能回退\n- 只背「unsafe 代码在 Unity 中何时使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 指针、性能、平台 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "32": {
    "answer": "- **结论**：NativeArray 用于 Job/Burst，需手动释放。 分配在 native allocator。\n- **考察点**：Native 内存、Job、Dispose，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：大量数据并行计算。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "分配在 native allocator。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "大量数据并行计算。 实现时可基于 `NativeArray<T>` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 忘记 Dispose——导致运行时异常或性能回退\n- 只背「NativeArray 与普通数组有什么区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Native 内存、Job、Dispose 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "33": {
    "answer": "- **结论**：可空注解帮助发现 null 风险，但 Unity 对象有特殊 null。 UnityEngine.Object 重载 ==。\n- **考察点**：null 安全、编译检查，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- **项目落地**：业务纯 C# 层开启。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "UnityEngine.Object 重载 ==。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "业务纯 C# 层开启。 实现时可基于 `IEnumerator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 以为完全避免 NRE——导致运行时异常或性能回退\n- 只背「C# 可空引用类型在 Unity 中如何看待」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 null 安全、编译检查 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "34": {
    "answer": "- **结论**：中大型项目可局部使用 DI，避免硬编码依赖。 通过接口注入稳定边界。\n- **考察点**：解耦、测试、服务，面试时应先给出判断标准再展开。\n- 结合 `async/await` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：战斗、网络、配置服务。\n- 用 Deep Profile 对比优化前后 `GC.Alloc`，目标战斗帧 0B。",
    "principle": "通过接口注入稳定边界。 CLR 分代 GC 在 Unity 主线程触发时会形成帧尖刺，因此热路径零分配是移动端硬指标。",
    "project": "战斗、网络、配置服务。 实现时可基于 `async/await` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全项目容器化过度设计——导致运行时异常或性能回退\n- 只背「依赖注入在 Unity 中有必要吗」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 解耦、测试、服务 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "35": {
    "answer": "- **结论**：统一命名、生命周期、空值、事件、日志和资源释放规范。 规范降低协作成本。\n- **考察点**：可维护性、性能、团队协作，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：Code Review 和 Roslyn 检查。\n- 热路径代码放独立 Assembly Definition，便于 Burst/测试与主工程隔离。",
    "principle": "规范降低协作成本。 C# 语义与 Unity 引擎对象（UnityEngine.Object）的销毁语义叠加，判空需用 `== null` 重载。",
    "project": "Code Review 和 Roslyn 检查。 实现时可基于 `UniTask` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只写文档不落地——导致运行时异常或性能回退\n- 只背「如何写出适合 Unity 的 C# 代码规范」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 可维护性、性能、团队协作 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "36": {
    "answer": "- **结论**：初始化用 Awake/OnEnable/Start 分层，帧循环用 FixedUpdate/Update/LateUpdate，销毁前务必 OnDisable/OnDestroy 解绑。\n- **完整顺序**：`Awake` → `OnEnable` → `Start` → 帧循环（`FixedUpdate` → `Update` → `LateUpdate`）→ `OnDisable` → `OnDestroy`。\n- **Awake**：脚本实例化时调用，适合 `GetComponent` 缓存、读 `[SerializeField]` 配置；即使 GameObject 未激活也会执行（若脚本 enabled）。\n- **OnEnable/Start**：`OnEnable` 在激活时触发且早于同帧 `Start`；应用 `SetActive(true)` 会再次 OnEnable。`Start` 适合依赖其他对象 Awake 完成的逻辑。\n- **帧循环**：`FixedUpdate` 固定步长驱动 `Rigidbody`；`Update` 读输入；`LateUpdate` 跟相机/看背。\n- **Unity 应用**：Script Execution Order 可微调同类函数先后；DontDestroyOnLoad 对象跨场景时 OnDisable/OnEnable 仍按激活状态触发。",
    "principle": "Unity 将 MonoBehaviour 回调注册进 PlayerLoop 的不同阶段：Awake/Start 属于初始化阶段，Update 族属于 Simulation 阶段。OnEnable 与 Start 的分离使得对象池复用时可以在不重建组件的情况下重新订阅事件。",
    "project": "`PlayerSpawnPoint.Start()` 里 `yield return new WaitForFixedUpdate()`  deliberately 晚于玩家 `Rigidbody` 初始化，避免与物理写入冲突；若把定位逻辑放 Awake，可能被玩家 Controller 同帧覆盖位置。",
    "mistakes": "- 在 Awake 里访问其他对象的 Start 才赋值的字段——得到 null 或默认值，偶现初始化顺序 bug。\n- OnEnable 订阅事件但 OnDisable 不解绑——对象池回收后仍收到回调，造成空引用或重复执行。\n- 在 OnDestroy 里调用 `Destroy(other)` 或访问已销毁单例——退出 Play 模式或切场景时抛 MissingReferenceException。"
  },
  "37": {
    "answer": "- **结论**：输入在 Update，物理在 FixedUpdate，相机在 LateUpdate。 三者调度频率不同。\n- **考察点**：时间步、物理、相机，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `Rigidbody.MovePosition` 在 FixedUpdate 中与 PhysX 同步；Update 里改 transform 会导致视觉抖动。\n- **项目落地**：输入缓存后物理移动。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "三者调度频率不同。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "输入缓存后物理移动。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- FixedUpdate 读按键——导致运行时异常或性能回退\n- 只背「Update、FixedUpdate、LateUpdate 如何分工」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 时间步、物理、相机 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "38": {
    "answer": "- **结论**：Prefab 管模板，Scene 管空间，SO 管配置。 资产和实例职责不同。\n- **考察点**：资源组织，面试时应先给出判断标准再展开。\n- Prefab Variant 继承父 Prefab 差异，适合 10 种怪物共用 BaseEnemy 逻辑。\n- ScriptableObject 存全局配置（技能表、DungeonDatabase），Scene 只放关卡实例与引用。\n- **项目落地**：怪物模板加配置表。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "资产和实例职责不同。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "怪物模板加配置表。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时改共享 SO——导致运行时异常或性能回退\n- 只背「Prefab、Scene、ScriptableObject 如何分工」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源组织 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "39": {
    "answer": "- **结论**：默认序列化字段和常见类型，多态与字典需特殊处理。 Unity 序列化服务编辑器资产。\n- **考察点**：Inspector、字段、引用，面试时应先给出判断标准再展开。\n- Unity 序列化走字段反射，不执行 property setter；`[FormerlySerializedAs]` 可兼容字段重命名。\n- `[SerializeReference]` 支持多态引用序列化，适合技能/对话节点树。\n- **项目落地**：List 包装字典。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "Unity 序列化服务编辑器资产。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "List 包装字典。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 属性以为会保存——导致运行时异常或性能回退\n- 只背「Unity 序列化系统有哪些限制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Inspector、字段、引用 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "40": {
    "answer": "- **结论**：物理对象用 Rigidbody API，Transform 会绕过求解。 直接改位置影响碰撞稳定。\n- **考察点**：物理、碰撞，面试时应先给出判断标准再展开。\n- `Rigidbody.MovePosition` 在 FixedUpdate 中与 PhysX 同步；Update 里改 transform 会导致视觉抖动。\n- `Rigidbody.interpolation` 可在渲染帧插值物理位置，改善相机跟随观感。\n- **项目落地**：箱子用 Rigidbody。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "直接改位置影响碰撞稳定。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "箱子用 Rigidbody。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 两套系统同时控制——导致运行时异常或性能回退\n- 只背「Rigidbody 移动和 Transform 移动差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 物理、碰撞 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "41": {
    "answer": "- **结论**：少量启动服务常驻并防重复。 常驻对象进入特殊场景。\n- **考察点**：常驻对象、场景，面试时应先给出判断标准再展开。\n- 结合 `Transform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：GameRoot、AudioManager。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "常驻对象进入特殊场景。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "GameRoot、AudioManager。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每场景放 Manager——导致运行时异常或性能回退\n- 只背「DontDestroyOnLoad 如何正确使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 常驻对象、场景 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "42": {
    "answer": "- **结论**：运行时频繁 Find 成本高且脆弱。 按名称遍历层级。\n- **考察点**：查找成本、耦合，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：启动时缓存引用。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "按名称遍历层级。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "启动时缓存引用。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Update 中 Find——O(n) 遍历 + 字符串比较，切场景后可能找到错误实例\n- 只背「GameObject.Find 有什么问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 查找成本、耦合 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "43": {
    "answer": "- **结论**：偶尔调用可以，热路径缓存。 需在组件列表中查找。\n- **考察点**：组件查找、缓存，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Awake 缓存常用组件。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "需在组件列表中查找。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "Awake 缓存常用组件。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每帧大量调用——导致运行时异常或性能回退\n- 只背「GetComponent 的成本如何看」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 组件查找、缓存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "44": {
    "answer": "- **结论**：`Time.timeScale = 0` 暂停「缩放时间域」，影响 `deltaTime`/`time`/`WaitForSeconds`；UI 动画和真实计时需用 unscaled 系列 API。\n- **受影响**：`Time.deltaTime`、`Time.time`、`Time.fixedTime`（乘 scale）、协程 `WaitForSeconds`、`Animator.speed`（若 updateMode 为 Normal）、粒子 simulationSpeed 等。\n- **不受影响**：`Time.unscaledDeltaTime`、`Time.realtimeSinceStartup`、`WaitForSecondsRealtime`、`DateTime.Now`、网络 IO 超时。\n- **物理注意**：`Time.fixedDeltaTime` 本身不随 scale 变，但 FixedUpdate 调用次数会随 scale 减少；Rigidbody 在 scale=0 时仍可能因碰撞求解产生微位移，需 `Rigidbody.Sleep()` 或 `simulationMode`。\n- **Unity 应用**：暂停菜单 DOTween 用 `SetUpdate(true)`；战斗倒计时 UI 用 `unscaledDeltaTime` 累加；子弹时间设 `timeScale=0.3` 并同步 `Time.fixedDeltaTime = 0.02f * timeScale`。",
    "principle": "Unity 将游戏逻辑时间与墙钟时间分离：timeScale 是游戏时钟倍率，所有基于 `Time.time` 积分的系统共享同一缩放因子。Animator 的 UnscaledTime 模式则绑定 `Time.unscaledDeltaTime`，因此可在暂停时继续播放 UI 过渡。",
    "project": "RPG 暂停界面：`PauseMenu` 设 `Time.timeScale=0`，同时 `CanvasGroup` 淡入用 `UIAnimation` + unscaled 时间；恢复战斗时还原 scale 与 fixedDeltaTime，避免物理步长不一致导致角色穿透。",
    "mistakes": "- 暂停后 UI 动画/Loading 圈也停住——玩家以为卡死，因协程仍用 `WaitForSeconds`。\n- 只改 timeScale 不改 Animator updateMode——角色 idle 动画冻结但状态机 Transition 仍依赖 unscaled 参数，表现不一致。\n- 多人游戏客户端本地 timeScale=0 但服务器时间继续走——重连后状态不同步，需区分本地表现暂停与逻辑暂停。"
  },
  "45": {
    "answer": "- **结论**：CharacterController 非真实物理，Rigidbody 参与物理。 Kinematic 碰撞由控制器处理。\n- **考察点**：角色控制、碰撞，面试时应先给出判断标准再展开。\n- `Rigidbody.MovePosition` 在 FixedUpdate 中与 PhysX 同步；Update 里改 transform 会导致视觉抖动。\n- `Rigidbody.interpolation` 可在渲染帧插值物理位置，改善相机跟随观感。\n- **项目落地**：ARPG 主角常用 KCC。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "Kinematic 碰撞由控制器处理。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "ARPG 主角常用 KCC。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 期望 CC 受力——导致运行时异常或性能回退\n- 只背「CharacterController 和 Rigidbody Controller 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 角色控制、碰撞 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "46": {
    "answer": "- **结论**：Layer 用于物理和渲染过滤，Tag 用于对象标识。 Layer 是位掩码更高效。\n- **考察点**：分类、碰撞、查询，面试时应先给出判断标准再展开。\n- `Rigidbody.MovePosition` 在 FixedUpdate 中与 PhysX 同步；Update 里改 transform 会导致视觉抖动。\n- `Rigidbody.interpolation` 可在渲染帧插值物理位置，改善相机跟随观感。\n- **项目落地**：敌人层、地面层。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "Layer 是位掩码更高效。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "敌人层、地面层。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Tag 承载复杂逻辑——导致运行时异常或性能回退\n- 只背「Layer 和 Tag 如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分类、碰撞、查询 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "47": {
    "answer": "- **结论**：使用 LayerMask、NonAlloc、限制距离。 过滤越早成本越低。\n- **考察点**：射线、GC、LayerMask，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：攻击检测、点击地面。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "过滤越早成本越低。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "攻击检测、点击地面。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- RaycastAll 每帧分配——导致运行时异常或性能回退\n- 只背「Physics.Raycast 如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 射线、GC、LayerMask 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "48": {
    "answer": "- **结论**：Trigger 只做 overlap 检测不产生物理阻挡；Collision 会解算接触力。拾取、区域检测用 Trigger，推拉、碰撞反馈用 Collision。\n- **Trigger 条件**：至少一方 Collider 勾选 `Is Trigger`，且至少一方挂 `Rigidbody`（可为 Kinematic）；双方都是无 Rigidbody 的静态 Collider 时 **不会** 触发 `OnTriggerEnter`。\n- **Collision 条件**：双方 `Is Trigger=false`，至少一方有 Rigidbody；回调提供 `Collision.contacts` 接触点与 `relativeVelocity`。\n- **Kinematic 用法**：玩家用 Kinematic Rigidbody + CharacterController/脚本移动，Trigger 区域挂静态 Collider，可稳定收到 `OnTriggerEnter`。\n- **Unity 应用**：掉落物：`SphereCollider(isTrigger=true)` + 玩家 Kinematic RB；墙壁：`BoxCollider` + Collision 播放音效；Layer Matrix 过滤 `Physics.IgnoreLayerCollision`。",
    "principle": "PhysX 对 Trigger 走 Broadphase/Narrowphase 重叠检测但不生成 Contact Manifold，因此无摩擦、无反弹；Collision 会生成接触约束并进入求解器。Rigidbody 存在是为了让 PhysX 将对象纳入动态/kinematic 模拟树，静态-静态 overlap 默认不派发消息以省 CPU。",
    "project": "Dungeon 入口：`PortalTrigger : MonoBehaviour` 在 `OnTriggerEnter(Collider other)` 检查 `other.CompareTag(\"Player\")` 后加载场景；若忘记给玩家加 Rigidbody，编辑器里看似重叠却永远不进触发器，策划报「传送门无效」。",
    "mistakes": "- 拾取物与玩家都无 Rigidbody——OnTriggerEnter 永不触发，测试 Scene 里拖动物体靠近也无法复现。\n- Trigger 与 Collision 混用同一 Collider 未勾选 isTrigger——角色被门挡住而不是穿过触发。\n- 在 OnTriggerStay 每帧 Instantiate 特效——CPU 和 GC 爆炸，应加 flag 或 OnTriggerEnter 单次处理。"
  },
  "49": {
    "answer": "- **结论**：通过 Transform 和 Camera API 转换。 不同空间单位和原点不同。\n- **考察点**：世界、本地、屏幕、视口，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：UI 跟随 3D 对象。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "不同空间单位和原点不同。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "UI 跟随 3D 对象。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 混用屏幕和世界坐标——导致运行时异常或性能回退\n- 只背「Unity 坐标空间有哪些」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 世界、本地、屏幕、视口 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "50": {
    "answer": "- **结论**：Quaternion 表示旋转更稳定，Euler 易读但可能万向锁。 四元数避免欧拉插值问题。\n- **考察点**：旋转、万向锁，面试时应先给出判断标准再展开。\n- 结合 `MonoBehaviour` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：相机旋转、角色朝向。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "四元数避免欧拉插值问题。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "相机旋转、角色朝向。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接改 euler 累积——导致运行时异常或性能回退\n- 只背「Quaternion 和 Euler 的区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 旋转、万向锁 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "51": {
    "answer": "- **结论**：频繁使用会造成 CPU 和内存压力。 克隆组件和资源引用，Destroy 延迟销毁。\n- **考察点**：实例化、销毁、GC，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：子弹和特效对象池。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "克隆组件和资源引用，Destroy 延迟销毁。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "子弹和特效对象池。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 战斗中大量 Instantiate——导致运行时异常或性能回退\n- 只背「Instantiate 和 Destroy 的性能问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 实例化、销毁、GC 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "52": {
    "answer": "- **结论**：Coroutine 适合复杂流程，Invoke 适合简单延迟。 都由 Unity 主线程调度。\n- **考察点**：延迟、流程控制，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能阶段用 Coroutine。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "都由 Unity 主线程调度。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "技能阶段用 Coroutine。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 字符串 Invoke 难维护——导致运行时异常或性能回退\n- 只背「Coroutine 和 Invoke 如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 延迟、流程控制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "53": {
    "answer": "- **结论**：LoadSceneAsync 可异步加载，allowSceneActivation 控制激活。 0.9 后等待激活。\n- **考察点**：场景加载、进度，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- Prefab Variant 继承父 Prefab 差异，适合 10 种怪物共用 BaseEnemy 逻辑。\n- **项目落地**：加载界面控制入场。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "0.9 后等待激活。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "加载界面控制入场。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 误解 progress 100%——导致运行时异常或性能回退\n- 只背「SceneManager 异步加载如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 场景加载、进度 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "54": {
    "answer": "- **结论**：叠加加载多个场景，便于分区和常驻服务。 多 Scene 同时存在。\n- **考察点**：多场景、流式加载，面试时应先给出判断标准再展开。\n- Prefab Variant 继承父 Prefab 差异，适合 10 种怪物共用 BaseEnemy 逻辑。\n- ScriptableObject 存全局配置（技能表、DungeonDatabase），Scene 只放关卡实例与引用。\n- **项目落地**：大地图分块、UI 场景。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "多 Scene 同时存在。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "大地图分块、UI 场景。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 未设置 ActiveScene——导致运行时异常或性能回退\n- 只背「Additive Scene 的应用场景」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 多场景、流式加载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "55": {
    "answer": "- **结论**：静态光照烘焙省性能，动态光表达变化。 光照计算从运行时转离线。\n- **考察点**：烘焙、实时光，面试时应先给出判断标准再展开。\n- 结合 `MonoBehaviour` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：主城静态烘焙。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "光照计算从运行时转离线。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "主城静态烘焙。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有灯实时——导致运行时异常或性能回退\n- 只背「Lightmap 和动态光如何取舍」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 烘焙、实时光 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "56": {
    "answer": "- **结论**：通过 Depth、Layer、URP Camera Stack 控制。 多相机叠加影响性能。\n- **考察点**：Depth、CullingMask、Stack，面试时应先给出判断标准再展开。\n- 结合 `Transform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：UI 相机、特效相机。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "多相机叠加影响性能。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "UI 相机、特效相机。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 多相机无必要叠加——导致运行时异常或性能回退\n- 只背「Camera 渲染顺序如何控制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Depth、CullingMask、Stack 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "57": {
    "answer": "- **结论**：URP 基于 SRP，可配置渲染特性，移动端友好。 渲染流程由管线资产控制。\n- **考察点**：渲染管线、SRP，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：移动项目常用 URP。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "渲染流程由管线资产控制。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "移动项目常用 URP。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 迁移材质丢失——导致运行时异常或性能回退\n- 只背「URP 和 Built-in 管线差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 渲染管线、SRP 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "58": {
    "answer": "- **结论**：多关键字组合产生大量变体。编译组合指数增长。\n- **考察点**：变体、关键字、打包，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：收集 SVC 预热。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "编译组合指数增长。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "收集 SVC 预热。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- multi_compile 滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「Shader Variant 为什么会爆炸」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 变体、关键字、打包 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "59": {
    "answer": "- **结论**：material 会实例化材质，sharedMaterial 修改共享资产。 实例材质会打断合批。\n- **考察点**：材质实例、批次，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：运行时换色用 MPB。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "实例材质会打断合批。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "运行时换色用 MPB。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 误改 sharedMaterial——每次访问 `renderer.material` 实例化新材质，Draw Call 与内存双爆\n- 只背「Material 和 SharedMaterial 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 材质实例、批次 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "60": {
    "answer": "- **结论**：Addressables 是资源管理系统，底层可使用 AssetBundle。 Addressables 管地址和依赖。\n- **考察点**：资源包、上层系统，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：热更新资源。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "Addressables 管地址和依赖。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "热更新资源。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 混用释放混乱——导致运行时异常或性能回退\n- 只背「AssetBundle 和 Addressables 关系」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源包、上层系统 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "61": {
    "answer": "- **结论**：简单但难管理依赖和释放，不适合大项目核心资源。 Resources 资源进包且路径加载。\n- **考察点**：资源加载、包体，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：原型或少量内置资源。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "Resources 资源进包且路径加载。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "原型或少量内置资源。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全项目依赖 Resources——导致运行时异常或性能回退\n- 只背「Resources.Load 的问题是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源加载、包体 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "62": {
    "answer": "- **结论**：StreamingAssets 随包只读，persistentDataPath 可写持久化。 平台路径和访问方式不同。\n- **考察点**：路径、读写，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：内置表、下载资源。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "平台路径和访问方式不同。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "内置表、下载资源。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Android 直接 File 读 SA——导致运行时异常或性能回退\n- 只背「StreamingAssets 和 persistentDataPath 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 路径、读写 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "63": {
    "answer": "- **结论**：适合少量非关键设置，不适合复杂存档和安全数据。 底层平台实现不同。\n- **考察点**：简单持久化，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：音量、画质、账号提示。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "底层平台实现不同。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "音量、画质、账号提示。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 存金币和完整存档——导致运行时异常或性能回退\n- 只背「PlayerPrefs 适合存什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 简单持久化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "64": {
    "answer": "- **结论**：根据平台和画质档设置目标帧率。 影响渲染循环频率。\n- **考察点**：帧率、功耗，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：30/45/60 档位。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "影响渲染循环频率。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "30/45/60 档位。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有设备 60 帧——导致运行时异常或性能回退\n- 只背「Application.targetFrameRate 如何设置」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 帧率、功耗 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "65": {
    "answer": "- **结论**：新输入系统支持 Action、设备映射和重绑定。 输入从轮询转事件/Action。\n- **考察点**：输入抽象、多设备，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：手柄、键鼠、触屏。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "输入从轮询转事件/Action。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "手柄、键鼠、触屏。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 新旧混用混乱——导致运行时异常或性能回退\n- 只背「Input System 和旧 Input 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 输入抽象、多设备 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "66": {
    "answer": "- **结论**：Gizmos 用于场景调试显示，Handles 用于编辑器交互。 仅编辑器可视辅助。\n- **考察点**：编辑器可视化，面试时应先给出判断标准再展开。\n- 结合 `Transform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：攻击范围、出生点。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "仅编辑器可视辅助。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "攻击范围、出生点。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时依赖 Gizmos——导致运行时异常或性能回退\n- 只背「Gizmos 和 Handles 用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 编辑器可视化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "67": {
    "answer": "- **结论**：只对少数基础入口使用，业务依赖应显式初始化。 全局顺序难维护。\n- **考察点**：执行顺序，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：GameRoot 先初始化。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "全局顺序难维护。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "GameRoot 先初始化。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 大量脚本配置顺序——导致运行时异常或性能回退\n- 只背「Script Execution Order 什么时候用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 执行顺序 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "68": {
    "answer": "- **结论**：在场景加载前后执行静态初始化。不依赖场景对象。\n- **考察点**：启动钩子，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：日志、服务注册。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "不依赖场景对象。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "日志、服务注册。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 多次初始化未防护——导致运行时异常或性能回退\n- 只背「RuntimeInitializeOnLoadMethod 用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 启动钩子 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "69": {
    "answer": "- **结论**：Pause 表示后台前台，Focus 表示焦点变化。 平台回调时机不同。\n- **考察点**：移动端生命周期，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：暂停音频、保存状态。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "平台回调时机不同。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "暂停音频、保存状态。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只处理一个回调——导致运行时异常或性能回退\n- 只背「OnApplicationPause 和 OnApplicationFocus 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 移动端生命周期 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "70": {
    "answer": "- **结论**：Destroy 后 C# 引用仍在，但 == null 返回 true。 UnityEngine.Object 重载运算符。\n- **考察点**：fake null、native 对象，面试时应先给出判断标准再展开。\n- 结合 `MonoBehaviour` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：异步回调判空。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "UnityEngine.Object 重载运算符。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "异步回调判空。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用 object.ReferenceEquals 判断错——导致运行时异常或性能回退\n- 只背「Unity 对象的 null 为什么特殊」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 fake null、native 对象 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "71": {
    "answer": "- **结论**：明确拥有者、初始化、重置和释放。 生命周期混乱导致泄漏。\n- **考察点**：创建、启用、销毁，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：UI 页面和战斗实体。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "生命周期混乱导致泄漏。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "UI 页面和战斗实体。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 多处 Destroy 同一对象——导致运行时异常或性能回退\n- 只背「如何设计对象生命周期」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 创建、启用、销毁 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "72": {
    "answer": "- **结论**：Agent 控制位置旋转时避免同时手动改 Transform。 Agent 内部模拟路径和速度。\n- **考察点**：寻路、移动控制，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：NPC 巡逻、追击。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "Agent 内部模拟路径和速度。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "NPC 巡逻、追击。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 与 Root Motion 冲突——导致运行时异常或性能回退\n- 只背「NavMeshAgent 使用注意点」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 寻路、移动控制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "73": {
    "answer": "- **结论**：逻辑状态为主，Animator 接收参数播放表现。 Animator 不应决定核心逻辑。\n- **考察点**：表现状态、逻辑状态，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- **项目落地**：战斗状态机驱动动画。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "Animator 不应决定核心逻辑。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "战斗状态机驱动动画。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 读 Animator 当业务状态——导致运行时异常或性能回退\n- 只背「Animator 与脚本状态如何同步」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 表现状态、逻辑状态 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "74": {
    "answer": "- **结论**：AudioSource 播放音源，Mixer 管分组音量和效果。 混音器统一控制输出。\n- **考察点**：音频播放、混音，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：BGM、SFX、Voice 分组。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "混音器统一控制输出。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "BGM、SFX、Voice 分组。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每个音效 new 对象——导致运行时异常或性能回退\n- 只背「AudioSource 和 AudioMixer 如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 音频播放、混音 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "75": {
    "answer": "- **结论**：控制发射数、生命周期、透明叠加和碰撞。 粒子常受填充率限制。\n- **考察点**：粒子数量、材质、Overdraw，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- **项目落地**：技能特效分档。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "粒子常受填充率限制。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "技能特效分档。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 粒子碰撞全开——导致运行时异常或性能回退\n- 只背「粒子系统性能关注什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 粒子数量、材质、Overdraw 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "76": {
    "answer": "- **结论**：适合过场、演出和多轨道时间线。 通过 PlayableGraph 调度轨道。\n- **考察点**：剧情、编排，面试时应先给出判断标准再展开。\n- 结合 `Transform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：剧情动画、镜头。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "通过 PlayableGraph 调度轨道。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "剧情动画、镜头。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用 Timeline 做复杂业务——导致运行时异常或性能回退\n- 只背「Timeline 适合什么场景」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 剧情、编排 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "77": {
    "answer": "- **结论**：可代码构建动画和播放图。 比 Animator Controller 更动态。\n- **考察点**：动画混合、运行时图，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：换装、技能动作融合。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "比 Animator Controller 更动态。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "换装、技能动作融合。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 简单需求过度使用——导致运行时异常或性能回退\n- 只背「Playable API 有什么价值」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 动画混合、运行时图 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "78": {
    "answer": "- **结论**：Editor 代码放 Editor 目录或 Editor asmdef。 避免运行时包含编辑器 API。\n- **考察点**：编译、平台，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：工具脚本、导入器。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "避免运行时包含编辑器 API。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "工具脚本、导入器。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Runtime 引 UnityEditor——导致运行时异常或性能回退\n- 只背「Editor 和 Runtime 代码如何分离」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 编译、平台 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "79": {
    "answer": "- **结论**：拆分程序集、控制依赖和加快编译。 Unity 按程序集增量编译。\n- **考察点**：编译、依赖、模块化，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Runtime、Editor、Tests 分包。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "Unity 按程序集增量编译。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "Runtime、Editor、Tests 分包。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 循环引用——导致运行时异常或性能回退\n- 只背「Assembly Definition 有什么作用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 编译、依赖、模块化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "80": {
    "answer": "- **结论**：自研模块可做成 UPM 包或 Git 包。 包含 package.json 和 asmdef。\n- **考察点**：UPM、依赖、复用，面试时应先给出判断标准再展开。\n- 结合 `MonoBehaviour` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：公共 UI 框架、工具库。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "包含 package.json 和 asmdef。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "公共 UI 框架、工具库。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 项目复制粘贴——导致运行时异常或性能回退\n- 只背「Package Manager 如何管理自研包」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 UPM、依赖、复用 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "81": {
    "answer": "- **结论**：文本表、参数替换、字体 fallback、图片资源分区。 语言影响布局和资源。\n- **考察点**：文本、字体、图片，面试时应先给出判断标准再展开。\n- 结合 `Transform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：中文英文日文切换。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "语言影响布局和资源。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "中文英文日文切换。 实现时可基于 `Transform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- UI 写死文本——导致运行时异常或性能回退\n- 只背「如何处理多语言本地化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 文本、字体、图片 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "82": {
    "answer": "- **结论**：监听配置变更并重新加载，运行时刷新索引。 减少进 Play 成本。\n- **考察点**：编辑器效率、数据刷新，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能参数调试。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "减少进 Play 成本。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "技能参数调试。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时状态被覆盖——导致运行时异常或性能回退\n- 只背「如何做配置热重载」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 编辑器效率、数据刷新 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "83": {
    "answer": "- **结论**：客户端显示可本地倒计时，关键逻辑以服务器时间为准。 本地时间可被修改。\n- **考察点**：时间同步、防作弊，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：活动、体力恢复。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "本地时间可被修改。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "活动、体力恢复。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 信任 DateTime.Now——导致运行时异常或性能回退\n- 只背「如何处理游戏时间和服务器时间」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 时间同步、防作弊 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "84": {
    "answer": "- **结论**：统一封装 Debug，支持等级、模块、文件和远程上报。 日志是线上定位入口。\n- **考察点**：日志等级、上报，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：崩溃前日志缓存。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "日志是线上定位入口。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "崩溃前日志缓存。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 线上无日志或过多——导致运行时异常或性能回退\n- 只背「如何实现全局日志系统」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 日志等级、上报 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "85": {
    "answer": "- **结论**：分析引用链，拆公共依赖，避免双向硬引用。 循环引用导致卸载困难。\n- **考察点**：依赖、卸载，面试时应先给出判断标准再展开。\n- 结合 `MonoBehaviour` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：UI 和配置分离。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "循环引用导致卸载困难。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "UI 和配置分离。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Prefab 互相引用——导致运行时异常或性能回退\n- 只背「如何处理资源引用循环」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 依赖、卸载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "86": {
    "answer": "- **结论**：场景内关键对象（出生点、交互点、刷怪点）应在加载期注册到场景上下文或配置表，禁止运行时 `FindObjectOfType`/`GameObject.Find` 全局搜索。\n- **机制**：`PlayerSpawnPoint` 优先从 `DungeonDatabase.TryGetById(DungeonLaunchContext.DungeonId, out Entry)` 读表配置坐标；无表数据时退回场景里 `PlayerSpawnPoint` 自身的 `Transform`。\n- **时序**：`yield return new WaitForFixedUpdate()` 后再 `PlacePlayer`，并对 `Rigidbody` 清零 `linearVelocity`/`angularVelocity`，避免物理惯性把玩家甩飞。\n- **注册表模式**：可用 `SceneRegistry` 单例在 Awake 收集 `[RegisterInScene]` 组件，或 ScriptableObject 关卡索引 + 场景内 Marker 双通道。\n- **Unity 应用**：Additive 加载子场景时，每个子场景一个 `SpawnAuthoring` 写入 `DungeonDatabase`；跨场景 DontDestroyOnLoad 的玩家由 `PlayerRigSpawner` 统一放置。",
    "principle": "全局查找是 O(n) 场景遍历且无法缓存，每次调用还分配枚举器；表驱动 + 场景 Marker 将「查谁」与「放在哪」解耦，Editor 里 `DungeonSpawnAuthoring` 可可视化写回表，保证策划改出生点不必改代码。",
    "project": "RPG-Silent：`PlayerSpawnPoint` 序列化引用 `DungeonDatabase`，运行时读 `DungeonLaunchContext.DungeonId`；`DungeonSpawnAuthoringEditor` 在 Scene 视图拖动手柄把 Transform 写回 `DungeonDatabase.Entry.SpawnPosition/SpawnRotation`，实现「场景编辑即配表」。",
    "mistakes": "- 多处 `FindWithTag(\"Player\")`/`FindObjectOfType<PlayerSpawnPoint>()`——Duplicate 玩家或切场景后找到错误实例，出生点随机。\n- 在 Awake 立即设置 Transform 而玩家 Rigidbody 同帧还在初始化——下一 FixedUpdate 被物理覆盖，表现为「闪一下回到原点」。\n- 表与场景 Transform 双源不一致且无日志——策划改场景但未写回 `DungeonDatabase`，打包后坐标仍与 Editor 预览不同。"
  },
  "87": {
    "answer": "- **结论**：大世界需原点漂移或分块坐标，比较浮点用 epsilon。 浮点远离原点精度下降。\n- **考察点**：坐标、计算，面试时应先给出判断标准再展开。\n- 结合 `Rigidbody` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：大地图移动。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "浮点远离原点精度下降。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "大地图移动。 实现时可基于 `Rigidbody` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接 == 比较 float——导致运行时异常或性能回退\n- 只背「如何处理浮点精度问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 坐标、计算 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "88": {
    "answer": "- **结论**：单一职责、明确输入输出、少依赖全局。 Unity 组件适合组合。\n- **考察点**：组件设计、组合，面试时应先给出判断标准再展开。\n- 结合 `SceneManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：血条跟随、伤害触发器。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "Unity 组件适合组合。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "血条跟随、伤害触发器。 实现时可基于 `SceneManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 组件内写业务全流程——导致运行时异常或性能回退\n- 只背「如何做可复用组件」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 组件设计、组合 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "89": {
    "answer": "- **结论**：保存状态、显示加载、卸载旧场景、加载新场景、初始化、进入。 切换是多系统协作。\n- **考察点**：加载、卸载、状态，面试时应先给出判断标准再展开。\n- 结合 `ScriptableObject` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：主城到战斗。\n- 用 Prefab 变体与 ScriptableObject 配置减少场景 diff 冲突。",
    "principle": "切换是多系统协作。 UnityEngine.Object 的 identity 与 C# 引用分离，Destroyed 对象 C# 引用非 null 但 `== null` 为 true。",
    "project": "主城到战斗。 实现时可基于 `ScriptableObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 黑屏中做重 CPU 初始化——导致运行时异常或性能回退\n- 只背「如何设计场景切换流程」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 加载、卸载、状态 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "90": {
    "answer": "- **结论**：先分支备份，检查插件、渲染、序列化和平台构建。 Unity 升级可能改变导入和 API。\n- **考察点**：兼容、插件、资源，面试时应先给出判断标准再展开。\n- Unity 序列化走字段反射，不执行 property setter；`[FormerlySerializedAs]` 可兼容字段重命名。\n- `[SerializeReference]` 支持多态引用序列化，适合技能/对话节点树。\n- **项目落地**：LTS 升级。\n- 在 Editor Play Mode 用 Frame Debugger + Physics Debug 验证行为与预期一致。",
    "principle": "Unity 升级可能改变导入和 API。 PlayerLoop 各阶段顺序固定，脚本回调插入 native 模拟管线，错阶段操作会导致一帧延迟或穿透。",
    "project": "LTS 升级。 实现时可基于 `MonoBehaviour` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接升级主分支——导致运行时异常或性能回退\n- 只背「Unity 项目升级版本要注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 兼容、插件、资源 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "91": {
    "answer": "- **结论**：动态 UI 变化会触发 Canvas 重建，应拆分动静 Canvas。 脏标记扩大重建范围。\n- **考察点**：Rebuild、Batch，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：血条独立 Canvas。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "脏标记扩大重建范围。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "血条独立 Canvas。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全界面一个大 Canvas——导致运行时异常或性能回退\n- 只背「Canvas 重建为什么会卡」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Rebuild、Batch 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "92": {
    "answer": "- **结论**：只创建可见项并复用刷新。避免大量节点和布局。\n- **考察点**：虚拟列表、对象池，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：背包、排行榜。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "避免大量节点和布局。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "背包、排行榜。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次创建上千项——导致运行时异常或性能回退\n- 只背「ScrollRect 长列表如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 虚拟列表、对象池 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "93": {
    "answer": "- **结论**：关闭无交互图文 RaycastTarget。 遍历 Graphic 候选。\n- **考察点**：射线、RaycastTarget，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：装饰图关闭射线。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "遍历 Graphic 候选。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "装饰图关闭射线。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有 Text 可点击——导致运行时异常或性能回退\n- 只背「GraphicRaycaster 如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 射线、RaycastTarget 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "94": {
    "answer": "- **结论**：管加载、层级、打开参数、返回栈和释放。 页面职责与全局导航分离。\n- **考察点**：层级、生命周期，面试时应先给出判断标准再展开。\n- 结合 `TextMeshProUGUI` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Normal/Popup/Toast。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "页面职责与全局导航分离。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "Normal/Popup/Toast。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 页面互相 Find——O(n) 遍历 + 字符串比较，切场景后可能找到错误实例\n- 只背「UI 页面管理器如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 层级、生命周期 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "95": {
    "answer": "- **结论**：字体清晰、富文本、fallback 和图文混排强。 SDF 缩放保持边缘。\n- **考察点**：SDF、多语言，面试时应先给出判断标准再展开。\n- 结合 `Canvas` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：中文字体和表情。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "SDF 缩放保持边缘。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "中文字体和表情。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Atlas 无限制增长——导致运行时异常或性能回退\n- 只背「TextMeshPro 优势是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 SDF、多语言 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "96": {
    "answer": "- **结论**：使用 SafeArea、锚点和缩放策略。 设备比例和安全区不同。\n- **考察点**：SafeArea、CanvasScaler，面试时应先给出判断标准再展开。\n- 结合 `RectTransform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：顶栏避开刘海。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "设备比例和安全区不同。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "顶栏避开刘海。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 固定像素坐标——导致运行时异常或性能回退\n- 只背「UI 适配刘海屏如何做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 SafeArea、CanvasScaler 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "97": {
    "answer": "- **结论**：Anchor 定父容器约束，Pivot 定自身旋转缩放中心。 RectTransform 使用归一化锚点。\n- **考察点**：布局、变换，面试时应先给出判断标准再展开。\n- 结合 `GraphicRaycaster` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：底部按钮、弹窗缩放。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "RectTransform 使用归一化锚点。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "底部按钮、弹窗缩放。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 锚点全默认居中——导致运行时异常或性能回退\n- 只背「Anchor 和 Pivot 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 布局、变换 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "98": {
    "answer": "- **结论**：LayoutGroup 方便但频繁变动成本高。 布局会递归计算尺寸。\n- **考察点**：自动布局、重建，面试时应先给出判断标准再展开。\n- 结合 `ScrollRect` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：静态面板可用。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "布局会递归计算尺寸。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "静态面板可用。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 动画中驱动布局——导致运行时异常或性能回退\n- 只背「LayoutGroup 性能问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 自动布局、重建 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "99": {
    "answer": "- **结论**：与 LayoutGroup 组合可能引发布局反复。 尺寸依赖互相驱动。\n- **考察点**：自动尺寸、布局循环，面试时应先给出判断标准再展开。\n- 结合 `TextMeshProUGUI` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：文本气泡谨慎使用。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "尺寸依赖互相驱动。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "文本气泡谨慎使用。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 列表项全挂 Fitter——导致运行时异常或性能回退\n- 只背「ContentSizeFitter 使用风险」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 自动尺寸、布局循环 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "100": {
    "answer": "- **结论**：材质、贴图、裁剪、层级穿插都会打断。 UGUI 按深度和材质合批。\n- **考察点**：材质、贴图、Mask，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：图集统一材质。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "UGUI 按深度和材质合批。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "图集统一材质。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 多字体多材质——导致运行时异常或性能回退\n- 只背「UI 合批被打断的原因」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 材质、贴图、Mask 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "101": {
    "answer": "- **结论**：将同类 UI 图打入图集，减少贴图切换。 同图集可共享贴图。\n- **考察点**：图集、包体、合批，面试时应先给出判断标准再展开。\n- 结合 `RectTransform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：UI 公共图集。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "同图集可共享贴图。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "UI 公共图集。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 超大混合图集——导致运行时异常或性能回退\n- 只背「SpriteAtlas 如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 图集、包体、合批 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "102": {
    "answer": "- **结论**：RectMask2D 只支持矩形且更轻，Mask 支持图形遮罩。 Mask 可能增加模板操作。\n- **考察点**：裁剪、性能，面试时应先给出判断标准再展开。\n- 结合 `GraphicRaycaster` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：列表裁剪用 RectMask2D。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "Mask 可能增加模板操作。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "列表裁剪用 RectMask2D。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 滥用复杂 Mask——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「Mask 和 RectMask2D 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 裁剪、性能 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "103": {
    "answer": "- **结论**：使用 Tween 或 Animator，但要管理打断和生命周期。 动画与页面状态同步。\n- **考察点**：Tween、状态，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- **项目落地**：弹窗开关动画。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "动画与页面状态同步。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "弹窗开关动画。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 关闭时 tween 仍跑——导致运行时异常或性能回退\n- 只背「UI 动画如何实现更稳」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Tween、状态 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "104": {
    "answer": "- **结论**：加载完成后检查页面是否仍需要。异步结果可能迟到。\n- **考察点**：异步、状态机，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：快速打开关闭页面。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "异步结果可能迟到。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "快速打开关闭页面。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 回调访问已销毁对象——导致运行时异常或性能回退\n- 只背「UI 打开关闭异步竞态如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 异步、状态机 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "105": {
    "answer": "- **结论**：用 ViewModel 或 Presenter 管数据，View 只刷新显示。 降低业务和视图耦合。\n- **考察点**：MVVM、刷新，面试时应先给出判断标准再展开。\n- 结合 `Canvas` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：背包列表刷新。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "降低业务和视图耦合。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "背包列表刷新。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 业务逻辑写进按钮——导致运行时异常或性能回退\n- 只背「如何设计 UI 数据绑定」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 MVVM、刷新 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "106": {
    "answer": "- **结论**：遮罩、GraphicRaycaster、事件系统优先级共同控制。 顶层命中决定是否继续。\n- **考察点**：输入、层级，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：弹窗阻挡底层点击。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "顶层命中决定是否继续。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "弹窗阻挡底层点击。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 透明图未拦截——导致运行时异常或性能回退\n- 只背「UI 事件穿透如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 输入、层级 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "107": {
    "answer": "- **结论**：控制 alpha、interactable、blocksRaycasts。 影响整组 UI。\n- **考察点**：透明、交互，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：弹窗淡入、禁用区域。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "影响整组 UI。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "弹窗淡入、禁用区域。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只改 alpha 不关交互——导致运行时异常或性能回退\n- 只背「CanvasGroup 有什么用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 透明、交互 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "108": {
    "answer": "- **结论**：处理 UI 输入、选中、拖拽和事件派发。 将输入模块转换为事件。\n- **考察点**：输入事件、焦点，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：按钮、拖拽道具。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "将输入模块转换为事件。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "按钮、拖拽道具。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 场景多个 EventSystem——导致运行时异常或性能回退\n- 只背「EventSystem 的作用是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 输入事件、焦点 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "109": {
    "answer": "- **结论**：实现 BeginDrag、Drag、EndDrag 并处理坐标转换。 事件系统派发拖拽数据。\n- **考察点**：Pointer 接口，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：背包拖拽。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "事件系统派发拖拽数据。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "背包拖拽。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 未处理取消和越界——导致运行时异常或性能回退\n- 只背「UI 拖拽如何实现」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Pointer 接口 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "110": {
    "answer": "- **结论**：UI 优先判断，再做 3D Raycast，必要时 World Space Canvas。 输入需要分层处理。\n- **考察点**：坐标转换、射线，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：角色头顶血条。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "输入需要分层处理。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "角色头顶血条。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- UI 点击穿透攻击——导致运行时异常或性能回退\n- 只背「UI 与 3D 世界交互怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 坐标转换、射线 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "111": {
    "answer": "- **结论**：World Space UI 受相机、缩放、排序影响。 本质是场景几何体。\n- **考察点**：Canvas 模式、相机，面试时应先给出判断标准再展开。\n- 结合 `RectTransform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：头顶血条、交互提示。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "本质是场景几何体。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "头顶血条、交互提示。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 字太小或抖动——导致运行时异常或性能回退\n- 只背「世界空间 UI 注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Canvas 模式、相机 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "112": {
    "answer": "- **结论**：高频页面缓存，低频页面关闭释放。 缓存换速度，释放换内存。\n- **考察点**：缓存、释放，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：主界面常驻，活动页释放。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "缓存换速度，释放换内存。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "主界面常驻，活动页释放。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全部常驻——导致运行时异常或性能回退\n- 只背「UI 资源释放策略如何定」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 缓存、释放 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "113": {
    "answer": "- **结论**：用红点树或条件节点统一计算和传播。 父节点聚合子节点状态。\n- **考察点**：数据驱动、树结构，面试时应先给出判断标准再展开。\n- 结合 `ScrollRect` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：任务、背包、活动。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "父节点聚合子节点状态。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "任务、背包、活动。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 页面里散写红点——导致运行时异常或性能回退\n- 只背「UI 红点系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 数据驱动、树结构 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "114": {
    "answer": "- **结论**：预留弹性空间，必要时动态字号或换行。 不同语言长度差异大。\n- **考察点**：本地化、布局，面试时应先给出判断标准再展开。\n- 结合 `TextMeshProUGUI` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：按钮、标题、说明。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "不同语言长度差异大。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "按钮、标题、说明。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 文本溢出按钮——导致运行时异常或性能回退\n- 只背「UI 多语言文本长度如何适配」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 本地化、布局 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "115": {
    "answer": "- **结论**：使用 TMP SpriteAsset 或富文本标签。 字符和图片统一排版。\n- **考察点**：TMP SpriteAsset，面试时应先给出判断标准再展开。\n- 结合 `Canvas` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：聊天表情、道具图标。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "字符和图片统一排版。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "聊天表情、道具图标。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 图片基线不齐——导致运行时异常或性能回退\n- 只背「UI 图文混排如何做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 TMP SpriteAsset 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "116": {
    "answer": "- **结论**：按控件类型或页面统一播放点击和反馈音。 避免按钮脚本重复播放。\n- **考察点**：交互反馈、音频，面试时应先给出判断标准再展开。\n- 结合 `RectTransform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Button 扩展组件。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "避免按钮脚本重复播放。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "Button 扩展组件。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 同一点击多次播放——导致运行时异常或性能回退\n- 只背「UI 音效如何管理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 交互反馈、音频 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "117": {
    "answer": "- **结论**：使用高亮目标、镂空遮罩、事件转发或阻挡。 引导层控制输入路径。\n- **考察点**：引导、镂空、事件，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：强引导点击按钮。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "引导层控制输入路径。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "强引导点击按钮。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 坐标适配失败——导致运行时异常或性能回退\n- 只背「UI 新手引导遮罩如何实现」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 引导、镂空、事件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "118": {
    "answer": "- **结论**：维护打开栈，返回键关闭顶层可关闭弹窗。 栈表达导航顺序。\n- **考察点**：导航、返回，面试时应先给出判断标准再展开。\n- 结合 `ScrollRect` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Android 返回键。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "栈表达导航顺序。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "Android 返回键。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 弹窗之间互关——导致运行时异常或性能回退\n- 只背「UI 弹窗栈如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 导航、返回 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "119": {
    "answer": "- **结论**：合并资源、场景和初始化进度，必要时平滑显示。 真实进度分段权重。\n- **考察点**：异步、用户体验，面试时应先给出判断标准再展开。\n- 结合 `TextMeshProUGUI` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：战斗加载界面。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "真实进度分段权重。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "战斗加载界面。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 完全假进度卡住——导致运行时异常或性能回退\n- 只背「UI Loading 进度如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 异步、用户体验 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "120": {
    "answer": "- **结论**：监听分辨率和安全区变化，调整 RectTransform offset。 横竖屏和系统栏会变化。\n- **考察点**：SafeArea、复用，面试时应先给出判断标准再展开。\n- 结合 `Canvas` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：顶底栏适配。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "横竖屏和系统栏会变化。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "顶底栏适配。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只启动时计算一次——导致运行时异常或性能回退\n- 只背「UI 安全区组件如何封装」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 SafeArea、复用 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "121": {
    "answer": "- **结论**：渲染到纹理或读取屏幕，再保存和调用平台分享。 读屏可能等待 GPU。\n- **考察点**：RenderTexture、权限，面试时应先给出判断标准再展开。\n- 结合 `RectTransform` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：分享战绩图。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "读屏可能等待 GPU。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "分享战绩图。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 主线程卡顿——导致运行时异常或性能回退\n- 只背「UI 截图和分享如何做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 RenderTexture、权限 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "122": {
    "answer": "- **结论**：使用降采样 RT 或预模糊图，避免实时高成本模糊。模糊是采样密集操作。\n- **考察点**：后处理、RenderTexture，面试时应先给出判断标准再展开。\n- 结合 `GraphicRaycaster` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：弹窗背景。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "模糊是采样密集操作。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "弹窗背景。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全屏实时高斯模糊——导致运行时异常或性能回退\n- 只背「UI Blur 背景怎么优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 后处理、RenderTexture 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "123": {
    "answer": "- **结论**：页面状态 Idle、Loading、Shown、Closing 可避免竞态。 状态机限制非法转移。\n- **考察点**：页面状态、加载，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：异步页面。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "状态机限制非法转移。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "异步页面。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 重复打开关闭错乱——导致运行时异常或性能回退\n- 只背「UI 状态机有什么价值」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 页面状态、加载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "124": {
    "answer": "- **结论**：为关键控件提供稳定标识，自动点击和断言状态。 UI 测试依赖稳定路径。\n- **考察点**：测试、选择器，面试时应先给出判断标准再展开。\n- 结合 `TextMeshProUGUI` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：登录、购买流程。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "UI 测试依赖稳定路径。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "登录、购买流程。 实现时可基于 `TextMeshProUGUI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 依赖层级路径——导致运行时异常或性能回退\n- 只背「UI 自动化测试怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 测试、选择器 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "125": {
    "answer": "- **结论**：抽象颜色、字体、图片和材质主题表。 主题值与页面解耦。\n- **考察点**：主题、资源替换，面试时应先给出判断标准再展开。\n- 结合 `Canvas` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：节日皮肤、深色模式。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "主题值与页面解耦。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "节日皮肤、深色模式。 实现时可基于 `Canvas` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 页面硬编码颜色——导致运行时异常或性能回退\n- 只背「UI 皮肤系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 主题、资源替换 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "126": {
    "answer": "- **结论**：UI 发命令，业务产事件，UI 监听刷新。 避免双向直接依赖。\n- **考察点**：Presenter、事件，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：背包使用物品。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "避免双向直接依赖。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "背包使用物品。 实现时可基于 `RectTransform` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Button 里直接改全局——导致运行时异常或性能回退\n- 只背「UI 和业务事件如何解耦」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Presenter、事件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "127": {
    "answer": "- **结论**：查看 UI 贴图、字体 Atlas、缓存页面和实例数。 UI 常占大量纹理内存。\n- **考察点**：Texture、Font、Prefab，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：活动页关闭释放。\n- Safe Area 用 `Screen.safeArea` 驱动 `RectTransform.offsetMin/Max`。",
    "principle": "UI 常占大量纹理内存。 多 Canvas 分层（Static HUD / Dynamic Popup）可隔离重建范围。",
    "project": "活动页关闭释放。 实现时可基于 `GraphicRaycaster` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 字体图集暴涨——导致运行时异常或性能回退\n- 只背「UI 内存如何分析」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Texture、Font、Prefab 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "128": {
    "answer": "- **结论**：页面资源地址化，逻辑与配置版本兼容。 热更要求资源和代码解耦。\n- **考察点**：资源、脚本、配置，面试时应先给出判断标准再展开。\n- 结合 `ScrollRect` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：活动 UI 远程加载。\n- Profiler 勾选 UI Details，定位 `Canvas.SendWillRenderCanvases` 耗时。",
    "principle": "热更要求资源和代码解耦。 UGUI 是 retained-mode：改数据 → MarkDirty → Rebuild Mesh → Batch，CPU 成本在 Rebuild 而非 Draw。",
    "project": "活动 UI 远程加载。 实现时可基于 `ScrollRect` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端无对应脚本——导致运行时异常或性能回退\n- 只背「UI 框架如何支持热更新」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源、脚本、配置 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "129": {
    "answer": "- **结论**：表现放 Animator，业务逻辑放代码。 Animator 不适合承载规则。\n- **考察点**：状态机、分层，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：移动 BlendTree，攻击代码驱动。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "Animator 不适合承载规则。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "移动 BlendTree，攻击代码驱动。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- AnyState 滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「Animator Controller 如何避免复杂失控」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 状态机、分层 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "130": {
    "answer": "- **结论**：RootMotion 真实，代码位移可控，常混合。 两个控制源会冲突。\n- **考察点**：动画驱动、手感，面试时应先给出判断标准再展开。\n- 结合 `AnimatorOverrideController` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：翻滚用 RM，移动用代码。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "两个控制源会冲突。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "翻滚用 RM，移动用代码。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- RM 与 Agent 同控——导致运行时异常或性能回退\n- 只背「Root Motion 和代码位移如何取舍」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 动画驱动、手感 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "131": {
    "answer": "- **结论**：根据连续参数混合动画。减少状态和过渡。\n- **考察点**：动画混合，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：八方向移动。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "减少状态和过渡。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "八方向移动。 实现时可基于 `PlayableGraph` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 参数未归一——导致运行时异常或性能回退\n- 只背「Blend Tree 解决什么问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 动画混合 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "132": {
    "answer": "- **结论**：适合表现触发，核心判定需可校验。事件存于动画剪辑。\n- **考察点**：帧事件、维护，面试时应先给出判断标准再展开。\n- C# 事件 `event Action OnHit` 外部只能 `+=`/`-=`，触发必须在类内 `OnHit?.Invoke()`，防止外部恶意清空订阅。\n- `UnityAction` 与 `Action` 类似但可序列化，常用于 Inspector 持久化回调（如 Button.onClick）。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- **项目落地**：脚步声、命中窗。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "事件存于动画剪辑。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "脚步声、命中窗。 实现时可基于 `Animator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 函数字符串失效——导致运行时异常或性能回退\n- 只背「Animation Event 优缺点」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 帧事件、维护 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "133": {
    "answer": "- **结论**：Layer 叠加动作，Mask 限定骨骼。 骨骼权重混合。\n- **考察点**：分层、局部骨骼，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：边跑边射击。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "骨骼权重混合。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "边跑边射击。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Mask 配太宽——导致运行时异常或性能回退\n- 只背「Animator Layer 和 Avatar Mask 用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分层、局部骨骼 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "134": {
    "answer": "- **结论**：瞬时事件用 Trigger，状态用 Bool/Int，连续量用 Float。 参数驱动状态转移。\n- **考察点**：参数设计，面试时应先给出判断标准再展开。\n- Kinematic Rigidbody + Trigger Collider 是拾取/传送门标准组合；静态 Collider 对不会触发回调。\n- `Physics.IgnoreLayerCollision` 让玩家层与玩家层不碰撞但可与敌人 Collision。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：攻击 Trigger、速度 Float。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "参数驱动状态转移。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "攻击 Trigger、速度 Float。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Bool 表示一次性事件——导致运行时异常或性能回退\n- 只背「Trigger、Bool、Int、Float 参数怎么选」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 参数设计 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "135": {
    "answer": "- **结论**：CrossFade 平滑过渡，Play 直接跳转。 CrossFade 生成过渡权重。\n- **考察点**：过渡、播放，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：技能切动作。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "CrossFade 生成过渡权重。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "技能切动作。 实现时可基于 `PlayableGraph` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接 Play 导致突变——导致运行时异常或性能回退\n- 只背「Animator.CrossFade 和 Play 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 过渡、播放 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "136": {
    "answer": "- **结论**：适合动画状态进入退出的表现逻辑。 绑定到 Animator 状态。\n- **考察点**：状态回调，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：开关特效、音效。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "绑定到 Animator 状态。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "开关特效、音效。 实现时可基于 `Animator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 写核心业务——导致运行时异常或性能回退\n- 只背「StateMachineBehaviour 适合做什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 状态回调 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "137": {
    "answer": "- **结论**：Humanoid 可用 OnAnimatorIK 或 Animation Rigging。 IK 在动画后修正骨骼。\n- **考察点**：目标约束、手脚，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：手持武器、脚贴地。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "IK 在动画后修正骨骼。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "手持武器、脚贴地。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有骨骼都 IK——导致运行时异常或性能回退\n- 只背「IK 在 Unity 中如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 目标约束、手脚 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "138": {
    "answer": "- **结论**：Humanoid 支持重定向和人形 IK，Generic 更通用。 Avatar 定义人形映射。\n- **考察点**：骨骼重定向，面试时应先给出判断标准再展开。\n- 结合 `AnimatorOverrideController` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：人形角色用 Humanoid。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "Avatar 定义人形映射。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "人形角色用 Humanoid。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 非人形强用 Humanoid——导致运行时异常或性能回退\n- 只背「Humanoid 和 Generic 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 骨骼重定向 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "139": {
    "answer": "- **结论**：将源动画映射到目标 Avatar 人形骨架。 标准人体骨骼抽象。\n- **考察点**：Avatar、骨骼映射，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：多角色共用动作。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "标准人体骨骼抽象。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "多角色共用动作。 实现时可基于 `PlayableGraph` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Avatar T Pose 错——导致运行时异常或性能回退\n- 只背「动画重定向的原理是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Avatar、骨骼映射 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "140": {
    "answer": "- **结论**：压缩减包体但可能抖动和脚滑。曲线关键帧被简化。\n- **考察点**：精度、包体，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：移动端动作压缩。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "曲线关键帧被简化。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "移动端动作压缩。 实现时可基于 `Animator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全部最高压缩——导致运行时异常或性能回退\n- 只背「动画压缩会带来什么问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 精度、包体 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "141": {
    "answer": "- **结论**：不可见时可停止或简化动画更新。减少离屏骨骼计算。\n- **考察点**：裁剪、性能，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：远处 NPC。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "减少离屏骨骼计算。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "远处 NPC。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- UI 角色被裁掉——导致运行时异常或性能回退\n- 只背「Animator Culling Mode 有什么用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 裁剪、性能 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "142": {
    "answer": "- **结论**：骨骼数、曲线数、Layer、IK 都影响成本。 每帧计算骨骼矩阵。\n- **考察点**：骨骼、曲线、层，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：怪物 LOD。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "每帧计算骨骼矩阵。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "怪物 LOD。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 高骨骼小怪海量——导致运行时异常或性能回退\n- 只背「动画采样成本由什么决定」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 骨骼、曲线、层 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "143": {
    "answer": "- **结论**：通过事件或时间轴打开判定，并由逻辑校验。 表现帧和逻辑帧需对齐。\n- **考察点**：命中窗、事件，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：攻击第 N 帧开盒。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "表现帧和逻辑帧需对齐。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "攻击第 N 帧开盒。 实现时可基于 `PlayableGraph` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只靠碰撞回调——导致运行时异常或性能回退\n- 只背「动画和碰撞判定如何同步」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 命中窗、事件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "144": {
    "answer": "- **结论**：输入缓冲、取消窗口、技能状态机共同控制。动画只表现阶段。\n- **考察点**：输入缓冲、状态，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：普攻三连。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "动画只表现阶段。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "普攻三连。 实现时可基于 `Animator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Animator 条件写连招——导致运行时异常或性能回退\n- 只背「连招系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 输入缓冲、状态 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "145": {
    "answer": "- **结论**：高优先级状态打断低优先级，表现层播放受击。 状态机控制可打断性。\n- **考察点**：状态优先级，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：PVE 怪物受击。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "状态机控制可打断性。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "PVE 怪物受击。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 任意状态都能打断——导致运行时异常或性能回退\n- 只背「受击、击退和硬直如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 状态优先级 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "146": {
    "answer": "- **结论**：过渡太长延迟响应，太短突兀。 过渡权重混合会延后目标姿态。\n- **考察点**：手感、响应，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：攻击起手短过渡。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "过渡权重混合会延后目标姿态。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "攻击起手短过渡。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 默认 0.25 全用——导致运行时异常或性能回退\n- 只背「动画过渡时间如何影响手感」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 手感、响应 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "147": {
    "answer": "- **结论**：保持状态机不变替换动画片段。复用控制器结构。\n- **考察点**：动作替换，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：不同武器动作。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "复用控制器结构。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "不同武器动作。 实现时可基于 `PlayableGraph` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时频繁生成——导致运行时异常或性能回退\n- 只背「Animator Override Controller 用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 动作替换 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "148": {
    "answer": "- **结论**：统一命名、Avatar、Loop、Root、压缩设置。 导入设置影响运行表现。\n- **考察点**：工程化、可维护，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：自动导入检查。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "导入设置影响运行表现。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "自动导入检查。 实现时可基于 `Animator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 手工设置易错——导致运行时异常或性能回退\n- 只背「动画资源命名和导入规范」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 工程化、可维护 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "149": {
    "answer": "- **结论**：用 Rig 约束实现瞄准、手部贴合、脚 IK。在动画结果上施加约束。\n- **考察点**：约束、运行时调整，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：武器握持。\n- Humanoid 复用动画：`Avatar` 一致即可 Retarget 共享 clip。",
    "principle": "在动画结果上施加约束。 Root Motion 位移写入 `Animator.deltaPosition`，与 CharacterController 叠加需明确优先级。",
    "project": "武器握持。 实现时可基于 `AnimatorController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 约束过多影响性能——导致运行时异常或性能回退\n- 只背「Animation Rigging 适合什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 约束、运行时调整 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "150": {
    "answer": "- **结论**：看 Animator 窗口、参数、当前状态、过渡和日志。 状态机可视化定位。\n- **考察点**：Debug、参数、状态，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：动作不播放排查。\n- Animator 窗口 Preview + `Animator.GetCurrentAnimatorStateInfo(0).shortNameHash` 调试状态。",
    "principle": "状态机可视化定位。 PlayableGraph 在 native 层评估，C# 每帧 Set 参数会触发状态机重评估，应事件驱动或 damp 过渡。",
    "project": "动作不播放排查。 实现时可基于 `AnimatorOverrideController` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看代码不看状态——导致运行时异常或性能回退\n- 只背「如何调试 Animator 问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Debug、参数、状态 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "151": {
    "answer": "- **结论**：支持地址、依赖、分组、远程和异步。 以 Catalog 管理资源定位。\n- **考察点**：资源管理、热更，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- **项目落地**：UI、角色、场景热更。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "以 Catalog 管理资源定位。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "UI、角色、场景热更。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有资源放 Resources——导致运行时异常或性能回退\n- 只背「Addressables 相比 Resources 优势」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源管理、热更 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "152": {
    "answer": "- **结论**：Release handle，实例用 ReleaseInstance。 引用计数归零释放依赖。\n- **考察点**：Handle、引用计数，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：UI 关闭释放。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "引用计数归零释放依赖。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "UI 关闭释放。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Destroy 不 Release——导致运行时异常或性能回退\n- 只背「Addressables 如何正确释放」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Handle、引用计数 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "153": {
    "answer": "- **结论**：按生命周期、更新频率、下载粒度分组。 Bundle 粒度影响加载和更新。\n- **考察点**：分包、依赖，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- **项目落地**：章节、角色、公共依赖。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "Bundle 粒度影响加载和更新。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "章节、角色、公共依赖。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一个大包或太碎——导致运行时异常或性能回退\n- 只背「Group 和 Bundle 策略怎么设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分包、依赖 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "154": {
    "answer": "- **结论**：检查 Catalog，下载新 Bundle，按地址加载。 Catalog 记录地址依赖 hash。\n- **考察点**：Catalog、CDN，面试时应先给出判断标准再展开。\n- 内部 `Entry[]` + 桶数组，负载因子超阈值时扩容为约 2 倍并重算 hash 索引。\n- 自定义 key 的 `GetHashCode()` 必须稳定：CombatEntity 作 key 时若 hash 随状态变会导致查不到。\n- **项目落地**：章节资源远程化。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "Catalog 记录地址依赖 hash。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "章节资源远程化。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只更新 Bundle——导致运行时异常或性能回退\n- 只背「远程更新流程是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Catalog、CDN 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "155": {
    "answer": "- **结论**：完成回调检查调用方是否仍有效。 请求结果可能迟到。\n- **考察点**：生命周期、取消，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：页面关闭后释放 handle。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "请求结果可能迟到。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "页面关闭后释放 handle。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 回调访问已销毁对象——导致运行时异常或性能回退\n- 只背「异步加载竞态如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 生命周期、取消 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "156": {
    "answer": "- **结论**：初始化加载运行时数据和 Catalog。 构建时生成定位数据。\n- **考察点**：RuntimeData、Catalog，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：启动阶段初始化。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "构建时生成定位数据。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "启动阶段初始化。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 首次加载才发现失败——导致运行时异常或性能回退\n- 只背「Addressables 初始化流程做什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 RuntimeData、Catalog 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "157": {
    "answer": "- **结论**：Label 用于批量定位和预下载资源。 一个资源可有多个标签。\n- **考察点**：标签、批量加载，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：预下载某章节。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "一个资源可有多个标签。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "预下载某章节。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 标签含义混乱——导致运行时异常或性能回退\n- 只背「Addressables Label 怎么用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 标签、批量加载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "158": {
    "answer": "- **结论**：Load 加载资源，Instantiate 加载并实例化。 实例释放方式不同。\n- **考察点**：资源、实例，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：Prefab 池化。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "实例释放方式不同。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "Prefab 池化。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Load 后忘记 Instantiate 管理——导致运行时异常或性能回退\n- 只背「LoadAssetAsync 和 InstantiateAsync 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源、实例 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "159": {
    "answer": "- **结论**：提前下载某地址或标签依赖。降低进入场景等待。\n- **考察点**：预下载、依赖，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：进战斗前下载资源。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "降低进入场景等待。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "进战斗前下载资源。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 下载后不处理失败——导致运行时异常或性能回退\n- 只背「DownloadDependenciesAsync 用途」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 预下载、依赖 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "160": {
    "answer": "- **结论**：查询未缓存依赖大小，用于提示玩家。 基于缓存和 Catalog 计算。\n- **考察点**：下载量、提示，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：章节下载确认。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "基于缓存和 Catalog 计算。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "章节下载确认。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 以为是资源总大小——导致运行时异常或性能回退\n- 只背「GetDownloadSizeAsync 的意义」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 下载量、提示 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "161": {
    "answer": "- **结论**：使用缓存 API 清理旧版本 Bundle。 缓存占磁盘需淘汰。\n- **考察点**：本地缓存、清理，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：资源版本回收。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "缓存占磁盘需淘汰。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "资源版本回收。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 永不清理缓存——导致运行时异常或性能回退\n- 只背「Addressables 缓存如何管理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 本地缓存、清理 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "162": {
    "answer": "- **结论**：允许 Catalog 远程更新资源定位信息。 客户端启动检查远程目录。\n- **考察点**：热更、远程目录，面试时应先给出判断标准再展开。\n- 结合 `ResourceManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：CDN 发布 Catalog。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "客户端启动检查远程目录。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "CDN 发布 Catalog。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 远程路径配置错——导致运行时异常或性能回退\n- 只背「Build Remote Catalog 有什么用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 热更、远程目录 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "163": {
    "answer": "- **结论**：区分本地、测试、正式 CDN 路径。 构建时替换变量。\n- **考察点**：环境配置，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：Dev/Test/Prod 环境。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "构建时替换变量。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "Dev/Test/Prod 环境。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 手改路径出错——导致运行时异常或性能回退\n- 只背「Addressables Profiles 如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 环境配置 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "164": {
    "answer": "- **结论**：基于上次构建状态生成内容更新包。 判断哪些 Bundle 需重建。\n- **考察点**：增量更新，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- **项目落地**：版本资源补丁。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "判断哪些 Bundle 需重建。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "版本资源补丁。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 没保存 build state——导致运行时异常或性能回退\n- 只背「Content Update Workflow 是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 增量更新 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "165": {
    "answer": "- **结论**：分开粒度细，合并减少请求和依赖复杂度。 影响包数量和更新范围。\n- **考察点**：Bundle Mode，面试时应先给出判断标准再展开。\n- 结合 `AsyncOperationHandle` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：独立大资源分开。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "影响包数量和更新范围。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "独立大资源分开。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全部 Separately 太碎——导致运行时异常或性能回退\n- 只背「Packed Separately 和 Together 如何选」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Bundle Mode 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "166": {
    "answer": "- **结论**：用 Analyze 查重复，抽公共依赖或调整分组。 共享依赖被多个包打入。\n- **考察点**：Analyze、依赖，面试时应先给出判断标准再展开。\n- 结合 `ResourceManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：公共材质贴图。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "共享依赖被多个包打入。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "公共材质贴图。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Prefab 隐式引用——导致运行时异常或性能回退\n- 只背「资源重复依赖如何发现和解决」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Analyze、依赖 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "167": {
    "answer": "- **结论**：LoadSceneAsync 加载，UnloadSceneAsync 卸载。 场景也由 handle 管理。\n- **考察点**：场景资源，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- Prefab Variant 继承父 Prefab 差异，适合 10 种怪物共用 BaseEnemy 逻辑。\n- **项目落地**：战斗场景远程加载。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "场景也由 handle 管理。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "战斗场景远程加载。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用 SceneManager 卸错——导致运行时异常或性能回退\n- 只背「Addressables Scene 如何加载卸载」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 场景资源 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "168": {
    "answer": "- **结论**：捕获 handle.Status，提示重试或回退默认资源。 网络和 CDN 都可能失败。\n- **考察点**：异常、重试，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：默认头像、占位图。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "网络和 CDN 都可能失败。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "默认头像、占位图。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 无错误提示卡死——导致运行时异常或性能回退\n- 只背「资源加载失败如何兜底」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 异常、重试 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "169": {
    "answer": "- **结论**：池持有实例和资源句柄，池销毁时统一释放。池对象生命周期长于单次使用。\n- **考察点**：实例、引用计数，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- **项目落地**：特效池。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "池对象生命周期长于单次使用。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "特效池。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 回池就 Release 资源——导致运行时异常或性能回退\n- 只背「Addressables 和对象池如何配合」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 实例、引用计数 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "170": {
    "answer": "- **结论**：封装 IAssetService，业务不直接依赖 Addressables。 降低替换和测试成本。\n- **考察点**：资源服务，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：UIManager 请求资源服务。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "降低替换和测试成本。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "UIManager 请求资源服务。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 业务散落 Addressables API——导致运行时异常或性能回退\n- 只背「Addressables 与依赖注入如何结合」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 资源服务 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "171": {
    "answer": "- **结论**：使用 HTTPS、hash、签名和版本校验。 客户端资源可被替换风险。\n- **考察点**：校验、篡改，面试时应先给出判断标准再展开。\n- 内部 `Entry[]` + 桶数组，负载因子超阈值时扩容为约 2 倍并重算 hash 索引。\n- 自定义 key 的 `GetHashCode()` 必须稳定：CombatEntity 作 key 时若 hash 随状态变会导致查不到。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- **项目落地**：CDN 资源保护。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "客户端资源可被替换风险。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "CDN 资源保护。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 明文敏感配置——导致运行时异常或性能回退\n- 只背「Addressables 安全性如何考虑」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 校验、篡改 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "172": {
    "answer": "- **结论**：关键资源优先，非关键资源后台下载。 下载带宽和 IO 竞争。\n- **考察点**：队列、体验，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：首屏 UI 优先。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "下载带宽和 IO 竞争。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "首屏 UI 优先。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次发起大量下载——导致运行时异常或性能回退\n- 只背「Addressables 加载优先级怎么处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 队列、体验 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "173": {
    "answer": "- **结论**：地址稳定、可读、与业务路径或 ID 对齐。 地址是运行时契约。\n- **考察点**：地址、可维护，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：ui/main/home_panel。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "地址是运行时契约。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "ui/main/home_panel。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 使用中文和随机名——导致运行时异常或性能回退\n- 只背「Addressables 资源命名规范」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 地址、可维护 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "174": {
    "answer": "- **结论**：定期扫描无引用、重复引用、大资源和非法引用。 工程规模大后手查不可行。\n- **考察点**：工具、依赖图，面试时应先给出判断标准再展开。\n- 结合 `ResourceManager` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：构建前检查。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "工程规模大后手查不可行。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "构建前检查。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 无用资源进包——导致运行时异常或性能回退\n- 只背「如何做资源引用审计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 工具、依赖图 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "175": {
    "answer": "- **结论**：编辑器可直接从 AssetDatabase，真机构建 Bundle。 模式不同会掩盖问题。\n- **考察点**：Play Mode Script，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：使用 Packed Play Mode 测试。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "模式不同会掩盖问题。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "使用 Packed Play Mode 测试。 实现时可基于 `Catalog` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只测 Fast Mode——导致运行时异常或性能回退\n- 只背「Addressables 在编辑器和真机表现差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Play Mode Script 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "176": {
    "answer": "- **结论**：DLC 用标签或 Catalog 分区，按需下载和卸载。 资源集可独立版本管理。\n- **考察点**：可选内容、下载，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：章节 DLC。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "资源集可独立版本管理。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "章节 DLC。 实现时可基于 `Addressables.LoadAssetAsync` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 主包依赖 DLC 资源——导致运行时异常或性能回退\n- 只背「Addressables 与 DLC 方案如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 可选内容、下载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "177": {
    "answer": "- **结论**：资源携带最低代码版本，客户端不兼容则强更或回退。 资源结构变化可能破坏旧代码。\n- **考察点**：版本协商，面试时应先给出判断标准再展开。\n- 结合 `AsyncOperationHandle` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：新 UI Prefab 组件缺失。\n- Content Update 后只打 changed bundle，客户端 `CheckForCatalogUpdates`。",
    "principle": "资源结构变化可能破坏旧代码。 Remote Catalog 更新可不改包体换资源地址，但需处理旧客户端与新 catalog 版本兼容。",
    "project": "新 UI Prefab 组件缺失。 实现时可基于 `AsyncOperationHandle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只更新资源不测旧包——导致运行时异常或性能回退\n- 只背「如何处理代码和资源版本不兼容」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 版本协商 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "178": {
    "answer": "- **结论**：记录地址、Catalog 版本、URL、hash、handle 异常。 远程问题需可追踪。\n- **考察点**：日志、Catalog、缓存，面试时应先给出判断标准再展开。\n- 内部 `Entry[]` + 桶数组，负载因子超阈值时扩容为约 2 倍并重算 hash 索引。\n- 自定义 key 的 `GetHashCode()` 必须稳定：CombatEntity 作 key 时若 hash 随状态变会导致查不到。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- **项目落地**：下载失败日志上报。\n- Event Viewer 观察 load/release；Analyze 面板查 duplicate bundle。",
    "principle": "远程问题需可追踪。 ResourceManager 引用计数 + Catalog 解析是加载内核；Release 未配对等价于 AB 泄漏。",
    "project": "下载失败日志上报。 实现时可基于 `ResourceManager` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只报加载失败——导致运行时异常或性能回退\n- 只背「Addressables 线上问题如何排查」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 日志、Catalog、缓存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "179": {
    "answer": "- **结论**：复现、采样、分型、定位、修改、验证。 数据驱动避免猜测。\n- **考察点**：Profiling、闭环，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Timeline 找峰值。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "数据驱动避免猜测。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "Timeline 找峰值。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看 FPS——导致运行时异常或性能回退\n- 只背「性能优化完整流程是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Profiling、闭环 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "180": {
    "answer": "- **结论**：Batch 减少绘制，SRP Batcher 减少 CPU 绑定成本。 状态变化打断批次。\n- **考察点**：渲染提交，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：URP 开 SRP Batcher。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "状态变化打断批次。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "URP 开 SRP Batcher。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只盯 DC 数——导致运行时异常或性能回退\n- 只背「Draw Call、Batch、SRP Batcher 关系」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 渲染提交 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "181": {
    "answer": "- **结论**：对比快照和引用链，查资源句柄和静态引用。 有引用就无法释放。\n- **考察点**：Memory Profiler，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：切场景后查残留。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "有引用就无法释放。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "切场景后查残留。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只 GC.Collect——导致运行时异常或性能回退\n- 只背「如何定位内存泄漏」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Memory Profiler 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "182": {
    "answer": "- **结论**：降帧、降分辨率、减后处理和高频计算。持续高负载触发降频。\n- **考察点**：功耗、热降频，面试时应先给出判断标准再展开。\n- 结合 `ObjectPool` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：画质档位。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "持续高负载触发降频。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "画质档位。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看短测 FPS——导致运行时异常或性能回退\n- 只背「移动端发热耗电如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 功耗、热降频 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "183": {
    "answer": "- **结论**：复用高频短生命周期对象。前置创建成本换少分配。\n- **考察点**：Instantiate、GC，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：子弹、特效。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "前置创建成本换少分配。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "子弹、特效。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 回收不重置——导致运行时异常或性能回退\n- 只背「对象池适合解决什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Instantiate、GC 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "184": {
    "answer": "- **结论**：预加载、异步、分帧、Shader 预热、Bundle 优化。 加载由多个阶段组成。\n- **考察点**：IO、解压、初始化，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- **项目落地**：战斗前预热。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "加载由多个阶段组成。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "战斗前预热。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 异步后一次性实例化——导致运行时异常或性能回退\n- 只背「加载耗时如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 IO、解压、初始化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "185": {
    "answer": "- **结论**：new、LINQ、字符串、装箱、闭包、数组返回。 托管对象进入 GC。\n- **考察点**：分配、Profiler，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：战斗帧零分配。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "托管对象进入 GC。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "战斗帧零分配。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Update 日志拼接——导致运行时异常或性能回退\n- 只背「GC Alloc 常见来源有哪些」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分配、Profiler 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "186": {
    "answer": "- **结论**：减少透明叠层、裁剪不可见像素、压缩 UI 层级。 每个透明像素多次绘制。\n- **考察点**：填充率、透明，面试时应先给出判断标准再展开。\n- 结合 `Frame Debugger` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：UI 背景和粒子。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "每个透明像素多次绘制。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "UI 背景和粒子。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 大面积透明图——导致运行时异常或性能回退\n- 只背「Overdraw 如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 填充率、透明 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "187": {
    "answer": "- **结论**：减少采样、分支、复杂光照和高精度。 GPU 执行片元计算。\n- **考察点**：ALU、纹理采样，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：移动端简化材质。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "GPU 执行片元计算。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "移动端简化材质。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一套 Shader 跑全机型——导致运行时异常或性能回退\n- 只背「Shader 性能如何分析」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ALU、纹理采样 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "188": {
    "answer": "- **结论**：减少关键字、剔除不用变体、预热常用变体。 变体影响包体和加载。\n- **考察点**：变体、编译，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：SVC 收集。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "变体影响包体和加载。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "SVC 收集。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- multi_compile 滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「Shader Variant 优化怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 变体、编译 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "189": {
    "answer": "- **结论**：查看主线程长耗时函数和调用栈。 Unity 大多 API 主线程执行。\n- **考察点**：Timeline、MainThread，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Canvas、Animator、Script。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "Unity 大多 API 主线程执行。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "Canvas、Animator、Script。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 盲目多线程——导致运行时异常或性能回退\n- 只背「CPU 主线程瓶颈如何定位」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Timeline、MainThread 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "190": {
    "answer": "- **结论**：渲染命令准备或驱动提交压力高。 CPU 渲染侧成本高。\n- **考察点**：渲染线程，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：材质状态切换多。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "CPU 渲染侧成本高。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "材质状态切换多。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只优化脚本——导致运行时异常或性能回退\n- 只背「RenderThread 瓶颈说明什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 渲染线程 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "191": {
    "answer": "- **结论**：降低分辨率若明显提升，多半 GPU 瓶颈。 GPU 受像素、带宽、Shader 影响。\n- **考察点**：GPU 时间、帧调试，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：后处理降级。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "GPU 受像素、带宽、Shader 影响。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "后处理降级。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 编辑器判断移动 GPU——导致运行时异常或性能回退\n- 只背「GPU 瓶颈如何判断」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 GPU 时间、帧调试 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "192": {
    "answer": "- **结论**：减少骨骼、离屏裁剪、LOD、合并材质。蒙皮每帧计算顶点。\n- **考察点**：SkinnedMeshRenderer，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- **项目落地**：大量怪物。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "蒙皮每帧计算顶点。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "大量怪物。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 小怪高骨骼——导致运行时异常或性能回退\n- 只背「骨骼动画性能如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 SkinnedMeshRenderer 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "193": {
    "answer": "- **结论**：简化碰撞体、层矩阵、降低 fixed 频率、NonAlloc 查询。 物理求解随对象和接触增加。\n- **考察点**：Collider、FixedUpdate，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- `Rigidbody.MovePosition` 在 FixedUpdate 中与 PhysX 同步；Update 里改 transform 会导致视觉抖动。\n- **项目落地**：攻击检测分层。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "物理求解随对象和接触增加。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "攻击检测分层。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- MeshCollider 滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「物理性能如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Collider、FixedUpdate 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "194": {
    "answer": "- **结论**：降低重算频率、分帧、LOD、局部避障简化。 寻路和避障都耗 CPU。\n- **考察点**：NavMesh、频率，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：怪物分批寻路。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "寻路和避障都耗 CPU。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "怪物分批寻路。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每帧 SetDestination——导致运行时异常或性能回退\n- 只背「AI 寻路性能如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 NavMesh、频率 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "195": {
    "answer": "- **结论**：拆 Canvas、虚拟列表、关 Raycast、少布局重建。 UI 卡顿常在重建。\n- **考察点**：Canvas、布局、射线，面试时应先给出判断标准再展开。\n- Canvas 重建：`Graphic.SetVerticesDirty()` → `Canvas.BuildBatch()`，频繁改 Text 会触发全 Canvas 重建。\n- 虚拟列表只实例化可见项 + `RectTransform.anchoredPosition` 复用，1000 条商品仍 20 个 Cell。\n- **项目落地**：背包优化。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "UI 卡顿常在重建。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "背包优化。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 大 Canvas 动态改——导致运行时异常或性能回退\n- 只背「UI 性能如何系统优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Canvas、布局、射线 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "196": {
    "answer": "- **结论**：按设备内存选择贴图、音频和特效档。 低端机内存和带宽有限。\n- **考察点**：纹理、音频、Mesh，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：低端 1K 高端 2K。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "低端机内存和带宽有限。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "低端 1K 高端 2K。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全机型同资源——导致运行时异常或性能回退\n- 只背「资源内存如何分档」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 纹理、音频、Mesh 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "197": {
    "answer": "- **结论**：Android 常用 ASTC/ETC2，按透明和质量选择。 压缩影响显存和带宽。\n- **考察点**：ASTC、ETC2、RGBA，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：UI 与场景分格式。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "压缩影响显存和带宽。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "UI 与场景分格式。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 使用未压缩 RGBA——导致运行时异常或性能回退\n- 只背「纹理压缩格式如何选」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ASTC、ETC2、RGBA 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "198": {
    "answer": "- **结论**：按音频长度与播放频率选 AudioClip Load Type——短 SFX 用 Decompress On Load，长 BGM/语音用 Streaming，中等长度权衡 Compressed In Memory。\n- **Decompress On Load**：导入时解压到 PCM，播放零解压延迟，适合 <1s 的 UI 点击、脚步；内存占用 = 样本数 × 声道 × 位深。\n- **Compressed In Memory**：保留压缩格式在内存，播放时 CPU 解压，适合中等音效；移动平台需关注 ADPCM/Vorbis 解码成本。\n- **Streaming**：磁盘逐块读取，内存恒定，适合 3~5 分钟 BGM；注意 Android 上 Streaming 路径与 `Application.streamingAssetsPath`。\n- **Unity 应用**：`AudioManager` 按组（BGM/SFX/Voice）设不同 Load Type；`AudioSource.PlayOneShot` 高频音效进池并共享 clip 引用，避免 duplicate clip。",
    "principle": "Unity 音频管线在加载期决定样本驻留形态：Decompress 将解压成本前移到加载，Streaming 将成本分摊到播放 IO。Load Type 与 Compression Format（PCM/Vorbis/ADPCM）共同决定内存、CPU 与延迟三角关系。",
    "project": "战斗项目：100+ 短音效（砍杀、受击）统一 Decompress On Load + `AudioSource` 对象池；章节 BGM 仅保留当前/下一首 Streaming clip 引用，切歌 `Resources.UnloadUnusedAssets` 释放旧流。",
    "mistakes": "- 全部 Decompress On Load——移动端音频内存轻松超 100MB，触发 Low Memory 杀进程。\n- Streaming BGM 却每帧 `Resources.Load` 新 clip——IO 卡顿且无法利用流式缓冲。\n- 3D 音效大量同时播放未设 `AudioSource.maxDistance`/优先级——混音器 Voice 数爆掉，全项目静音。"
  },
  "199": {
    "answer": "- **结论**：减顶点、合并子网格、去无用属性、LOD。 顶点处理和带宽成本。\n- **考察点**：顶点、三角、属性，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：场景道具 LOD。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "顶点处理和带宽成本。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "场景道具 LOD。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看三角不看顶点——导致运行时异常或性能回退\n- 只背「Mesh 优化关注什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 顶点、三角、属性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "200": {
    "answer": "- **结论**：根据屏幕占比切换模型和材质。远处不需要高精度。\n- **考察点**：LOD、距离，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：怪物、建筑。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "远处不需要高精度。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "怪物、建筑。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- LOD 切换突兀——导致运行时异常或性能回退\n- 只背「LODGroup 如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 LOD、距离 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "201": {
    "answer": "- **结论**：适合静态遮挡明显的场景。预计算可见性减少渲染。\n- **考察点**：遮挡剔除，面试时应先给出判断标准再展开。\n- 结合 `Frame Debugger` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：室内、城市。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "预计算可见性减少渲染。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "室内、城市。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 开放场景收益低——导致运行时异常或性能回退\n- 只背「Occlusion Culling 适合什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 遮挡剔除 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "202": {
    "answer": "- **结论**：Static Batching 合并静态网格降低 Draw Call，但会复制顶点缓冲增加内存；URP 项目应优先实测 SRP Batcher，再决定是否 Mark Static。\n- **机制**：勾选 Static 且满足同材质/同 Lightmap 等条件时，引擎在构建期或运行时将多个 Mesh 合成大 VBO，CPU 侧一次提交；动态物体无法参与。\n- **与 SRP Batcher**：SRP Batcher 通过 CBUFFER 复用材质数据减少 SetPassCall，两者解决不同瓶颈；部分 URP 配置下 Static Batching 收益被 SRP Batcher 部分替代，需 Frame Debugger 对比。\n- **内存代价**：合批后每份静态网格顶点可能被复制进合批 VBO，同材质 500 个草实例可能 +50MB 顶点内存。\n- **Unity 应用**：大地形建筑、关卡静态道具 Mark `Batching Static`；可交互门、可破坏箱不 Mark；GPU Instancing 适用的草/树优先 Instancing 而非 Static Batching。",
    "principle": "Static Batching 在 CPU 侧合并 Draw Call，代价是将原本共享 Mesh 的实例复制为合批几何体常驻 GPU/CPU 内存。SRP Batcher 则不合并网格，而是让不同 Mesh 在 shader 兼容时共享 per-object 大缓冲，因此移动 URP 项目常先开 SRP Batcher 再评估 Static。",
    "project": "开放世界手游：城区建筑 Prefab 统一材质图集 + Batching Static，Draw Call 从 800 降到 200，但 Memory Profiler 显示 Mesh 内存 +30MB；改 GPU Instancing 渲染重复路灯后 DC 持平且内存回落。",
    "mistakes": "- 可移动/可缩放对象误勾 Static——每帧 Transform 变化导致合批失效并触发重建，CPU 尖刺。\n- 不同 Lightmap 或不同材质却 Mark Static——无法合批，白占 Static 标记的维护成本。\n- 假设 Static Batching 与 SRP Batcher 收益叠加——实测 Draw Call 不降反升，因材质变体不兼容 SRP Batcher。"
  },
  "203": {
    "answer": "- **结论**：Dynamic Batching 仅适合顶点属性 ≤900 左右的小网格，CPU 合批开销大；URP 移动项目优先 SRP Batcher + GPU Instancing，勿指望 Dynamic Batching 救场。\n- **限制**：网格顶点过多、多 pass shader、不同 scale（非 uniform 缩放）、启用 lightmap 等都会打断动态合批。\n- **CPU 成本**：每帧在 CPU 侧拼顶点缓冲，物体多时合批本身成为瓶颈，Profiler 里 `Batching.Draw` 耗时上升。\n- **URP 现实**：2021+ URP 默认更依赖 SRP Batcher/GPU Instancing；大量小道具共用 `MaterialPropertyBlock` 改色是更稳方案。\n- **Unity 应用**：子弹、伤害数字等小 Quad 可合并 Atlas + 单材质 Instancing；Particle System 用 GPU 模式而非大量小 Mesh Renderer。",
    "principle": "Dynamic Batching 在运行时把多个小网格的顶点拷贝到临时缓冲再绘制，属于 CPU 侧优化；当 Draw Call 已不高或网格不够「小且同构」时，拷贝成本超过省下的 SetPassCall。Instancing 则在 GPU 侧一次 draw 多 instance，扩展性更好。",
    "project": "塔防项目：50 种小敌人本期望 Dynamic Batching，Profiler 显示主线程 Batching 占 4ms；改为共享 SkinnedMesh 材质 + GPU Instancing 后 DC 从 120→15，CPU 回落 2ms。",
    "mistakes": "- 高面数角色期望动态合批——顶点超限，每个角色仍单独 Draw Call。\n- 对 UI Canvas  Renderer 开 Dynamic Batching 幻想——UGUI 走不同渲染路径，与 3D Dynamic Batching 无关。\n- 材质实例化（`renderer.material`）破坏合批——应使用 `sharedMaterial` + MaterialPropertyBlock。"
  },
  "204": {
    "answer": "- **结论**：同 Mesh/Material 大量对象用 Instancing。 一次提交多个实例数据。\n- **考察点**：重复对象，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：草、石头、子弹。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "一次提交多个实例数据。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "草、石头、子弹。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 材质不一致——导致运行时异常或性能回退\n- 只背「GPU Instancing 适合什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 重复对象 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "205": {
    "answer": "- **结论**：用于大量纯计算并行和 SIMD 优化。 避免主线程串行计算。\n- **考察点**：并行、数据导向，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：寻路、碰撞预筛。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "避免主线程串行计算。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "寻路、碰撞预筛。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 在 Job 中调 Unity API——导致运行时异常或性能回退\n- 只背「Burst 和 Job System 优化什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 并行、数据导向 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "206": {
    "answer": "- **结论**：组件数据连续存储，便于 cache 和并行。减少对象指针跳转。\n- **考察点**：数据连续、批处理，面试时应先给出判断标准再展开。\n- 结合 `Frame Debugger` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：海量单位。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "减少对象指针跳转。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "海量单位。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 小项目全 ECS——导致运行时异常或性能回退\n- 只背「ECS 性能优势来自哪里」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 数据连续、批处理 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "207": {
    "answer": "- **结论**：短时间定位脚本调用可用，但开销很大。 插桩会改变性能表现。\n- **考察点**：深度采样，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：编辑器局部排查。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "插桩会改变性能表现。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "编辑器局部排查。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用 Deep 数据做最终结论——导致运行时异常或性能回退\n- 只背「Profiler Deep Profile 何时用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 深度采样 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "208": {
    "answer": "- **结论**：查看每个绘制调用、材质、Pass 和批次原因。 渲染问题逐 draw 定位。\n- **考察点**：渲染分析，面试时应先给出判断标准再展开。\n- 结合 `SRP Batcher` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：合批失败分析。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "渲染问题逐 draw 定位。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "合批失败分析。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看 Stats 面板——导致运行时异常或性能回退\n- 只背「Frame Debugger 有什么用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 渲染分析 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "209": {
    "answer": "- **结论**：看 Native/Managed/Object、纹理、Mesh、引用链。 快照比较找增量。\n- **考察点**：内存快照，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：切场景泄漏。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "快照比较找增量。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "切场景泄漏。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看总内存——导致运行时异常或性能回退\n- 只背「Memory Profiler 重点看什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 内存快照 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "210": {
    "answer": "- **结论**：将实例化、初始化、绑定拆成小任务逐帧执行。 限制单帧耗时。\n- **考察点**：分帧、任务队列，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：生成怪物队列。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "限制单帧耗时。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "生成怪物队列。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- for 循环一帧全做——导致运行时异常或性能回退\n- 只背「加载界面卡顿如何分帧」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分帧、任务队列 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "211": {
    "answer": "- **结论**：线上限制等级和采样，避免热路径拼接。 日志可能分配和写 IO。\n- **考察点**：IO、字符串，面试时应先给出判断标准再展开。\n- 结合 `Frame Debugger` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：战斗日志按开关。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "日志可能分配和写 IO。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "战斗日志按开关。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Debug.Log 每帧——导致运行时异常或性能回退\n- 只背「日志系统对性能影响」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 IO、字符串 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "212": {
    "answer": "- **结论**：降粒子、少透明层、合图集、按画质裁剪。 粒子多受 GPU 填充影响。\n- **考察点**：粒子数、材质，面试时应先给出判断标准再展开。\n- 结合 `ObjectPool` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能特效分档。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "粒子多受 GPU 填充影响。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "技能特效分档。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 碰撞和阴影全开——导致运行时异常或性能回退\n- 只背「粒子特效优化怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 粒子数、材质 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "213": {
    "answer": "- **结论**：移动端控制 Bloom、景深、SSAO、抗锯齿。 后处理多为全屏 Pass。\n- **考察点**：Bloom、AA、Color，面试时应先给出判断标准再展开。\n- 结合 `SRP Batcher` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：高端开 SSAO。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "后处理多为全屏 Pass。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "高端开 SSAO。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 低端全开后处理——导致运行时异常或性能回退\n- 只背「后处理性能如何控制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Bloom、AA、Color 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "214": {
    "answer": "- **结论**：降分辨率、距离、级联、投影对象数量。 阴影需要额外渲染深度。\n- **考察点**：ShadowMap、级联，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：主角和主光保留。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "阴影需要额外渲染深度。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "主角和主光保留。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有小物投影——导致运行时异常或性能回退\n- 只背「阴影性能如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ShadowMap、级联 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "215": {
    "answer": "- **结论**：烘焙优先，实时探针低频更新。实时反射要额外渲染。\n- **考察点**：ReflectionProbe，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：金属场景。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "实时反射要额外渲染。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "金属场景。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每帧实时探针——导致运行时异常或性能回退\n- 只背「反射和探针性能怎么处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ReflectionProbe 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "216": {
    "answer": "- **结论**：控制像素光数量，静态光烘焙，动态光分级。 多光源增加 Pass 或计算。\n- **考察点**：光照、Pass，面试时应先给出判断标准再展开。\n- 结合 `Frame Debugger` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能点光限时。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "多光源增加 Pass 或计算。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "技能点光限时。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 场景大量实时灯——导致运行时异常或性能回退\n- 只背「实时光源数量如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 光照、Pass 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "217": {
    "answer": "- **结论**：根据设备和压力调整渲染分辨率。 降低像素工作量。\n- **考察点**：RenderScale、动态分辨率，面试时应先给出判断标准再展开。\n- 结合 `ObjectPool` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：低端 0.8 RenderScale。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "降低像素工作量。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "低端 0.8 RenderScale。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- UI 也变糊——导致运行时异常或性能回退\n- 只背「移动端分辨率缩放怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 RenderScale、动态分辨率 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "218": {
    "answer": "- **结论**：为帧时、内存、DC、三角、加载设预算。 预算让团队有目标。\n- **考察点**：指标、团队协作，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：机型档位表。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "预算让团队有目标。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "机型档位表。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 事后才优化——导致运行时异常或性能回退\n- 只背「如何做性能预算」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 指标、团队协作 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "219": {
    "answer": "- **结论**：固定场景、固定路径、采集指标、对比阈值。 防止性能回退。\n- **考察点**：CI、基准，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：每日跑战斗场景。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "防止性能回退。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "每日跑战斗场景。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 手动测不稳定——导致运行时异常或性能回退\n- 只背「如何做自动化性能测试」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 CI、基准 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "220": {
    "answer": "- **结论**：找单帧耗时峰值，区分脚本、GC、加载、渲染。 平均 FPS 掩盖尖峰。\n- **考察点**：Spike、Timeline，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：战斗释放技能卡顿。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "平均 FPS 掩盖尖峰。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "战斗释放技能卡顿。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看平均帧率——导致运行时异常或性能回退\n- 只背「如何分析卡顿尖峰」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Spike、Timeline 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "221": {
    "answer": "- **结论**：预加载资源、Shader 预热、对象池预热、分帧初始化。 首次使用成本集中爆发。\n- **考察点**：预热、编译，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：进战前预热技能。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "首次使用成本集中爆发。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "进战前预热技能。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 首次释放技能编译——导致运行时异常或性能回退\n- 只背「如何减少首帧卡顿」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 预热、编译 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "222": {
    "answer": "- **结论**：会扫描未引用资源，可能产生明显卡顿。 需要检查对象引用关系。\n- **考察点**：卸载、扫描，面试时应先给出判断标准再展开。\n- `AsyncOperationHandle` 必须配对 `Addressables.Release(handle)`，否则 AssetBundle 常驻内存。\n- `await Addressables.LoadAssetAsync<T>(key).Task` 要处理同一 key 并发加载的竞态（缓存 handle）。\n- **项目落地**：切大场景时调用。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "需要检查对象引用关系。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "切大场景时调用。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 战斗中频繁调用——导致运行时异常或性能回退\n- 只背「Resources.UnloadUnusedAssets 的成本」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 卸载、扫描 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "223": {
    "answer": "- **结论**：一般避免，必要时在加载界面或低交互阶段触发。强制 GC 会卡顿。\n- **考察点**：手动 GC，面试时应先给出判断标准再展开。\n- Profiler Deep Profile 看 `GC.Alloc` 列：常见元凶是 `string` 拼接、`LINQ`、`foreach` 装箱枚举、lambda 捕获。\n- 对象池 `Pool<T>.Get()` 复用 `List<T>`/`StringBuilder` 可让战斗帧保持 0B GC。\n- **项目落地**：切场景后。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "强制 GC 会卡顿。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "切场景后。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每隔几秒 Collect——导致运行时异常或性能回退\n- 只背「GC.Collect 什么时候用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 手动 GC 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "224": {
    "answer": "- **结论**：后台读取和解析，主线程只接收结果。 避免主线程阻塞磁盘。\n- **考察点**：IO、线程，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- **项目落地**：存档、配置加载。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "避免主线程阻塞磁盘。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "存档、配置加载。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 后台线程调 Unity API——导致运行时异常或性能回退\n- 只背「异步文件 IO 如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 IO、线程 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "225": {
    "answer": "- **结论**：后台收包，主线程限量派发，减少反序列化分配。 网络峰值可能阻塞帧。\n- **考察点**：序列化、队列，面试时应先给出判断标准再展开。\n- Unity 序列化走字段反射，不执行 property setter；`[FormerlySerializedAs]` 可兼容字段重命名。\n- `[SerializeReference]` 支持多态引用序列化，适合技能/对话节点树。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- **项目落地**：战斗消息队列。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "网络峰值可能阻塞帧。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "战斗消息队列。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一帧处理全部消息——导致运行时异常或性能回退\n- 只背「网络包处理性能如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 序列化、队列 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "226": {
    "answer": "- **结论**：AI 分频、动画裁剪、渲染 LOD、碰撞简化、对象池。多系统叠加成本。\n- **考察点**：LOD、AI、动画，面试时应先给出判断标准再展开。\n- `Animator.SetFloat(\"Speed\", speed, dampTime, dt)` 平滑混合比每帧硬设参数更自然。\n- Animation Event 在指定帧调 `OnAttackHit()`，但删除/重导入 clip 后 event 可能丢失需校验。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：刷怪潮。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "多系统叠加成本。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "刷怪潮。 实现时可基于 `Frame Debugger` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只降模型面数——导致运行时异常或性能回退\n- 只背「大量怪物同屏如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 LOD、AI、动画 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "227": {
    "answer": "- **结论**：按距离和视野加载块，异步加载和卸载。控制内存和 IO 峰值。\n- **考察点**：Streaming、分块，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- **项目落地**：地图格子。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "控制内存和 IO 峰值。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "地图格子。 实现时可基于 `ObjectPool` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次加载整图——导致运行时异常或性能回退\n- 只背「大世界流式加载如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Streaming、分块 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "228": {
    "answer": "- **结论**：根据 CPU/GPU/内存设置画质、帧率和资源档。 不同设备能力差异大。\n- **考察点**：设备档位，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- **项目落地**：首次启动跑档位。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "不同设备能力差异大。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "首次启动跑档位。 实现时可基于 `SRP Batcher` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用户手动开高崩溃——导致运行时异常或性能回退\n- 只背「如何处理低端机降级」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 设备档位 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "229": {
    "answer": "- **结论**：使用同设备同场景对比优化前后指标。控制变量才可信。\n- **考察点**：A/B、数据，面试时应先给出判断标准再展开。\n- 结合 `BurstCompiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：帧时下降数据。\n- 低端机降 `QualitySettings.resolutionScalingFixedDPIFactor` 与粒子上限。",
    "principle": "控制变量才可信。 优化有效性用同场景同机型的 Profile 对比中位数帧时间，而非单次 FPS 截图。",
    "project": "帧时下降数据。 实现时可基于 `BurstCompiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只凭肉眼——导致运行时异常或性能回退\n- 只背「如何判断优化是否有效」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 A/B、数据 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "230": {
    "answer": "- **结论**：提供资源规范、检查工具和可视化反馈。 美术资源决定大量性能。\n- **考察点**：规范、预算，面试时应先给出判断标准再展开。\n- 结合 `Profiler` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：导入检查三角和贴图。\n- 建立帧预算：逻辑 4ms / 渲染 8ms / UI 2ms，每次只改一个变量对比 Profile。",
    "principle": "美术资源决定大量性能。 移动 GPU 常 fill-rate/bandwidth bound，降分辨率与 Overdraw 往往比减 Draw Call 更有效。",
    "project": "导入检查三角和贴图。 实现时可基于 `Profiler` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 口头要求无工具——导致运行时异常或性能回退\n- 只背「性能优化如何和美术协作」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 规范、预算 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "231": {
    "answer": "- **结论**：表现、业务、数据、基础设施分层。 依赖方向决定可维护性。\n- **考察点**：架构、依赖，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：战斗逻辑纯 C#。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "依赖方向决定可维护性。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "战斗逻辑纯 C#。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- MonoBehaviour 包万物——导致运行时异常或性能回退\n- 只背「Unity 项目如何模块分层」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 架构、依赖 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "232": {
    "answer": "- **结论**：OOP 管业务，组件化管组合，ECS 管海量数据。 不同范式服务不同问题。\n- **考察点**：范式、性能，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：海量弹幕局部 ECS。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "不同范式服务不同问题。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "海量弹幕局部 ECS。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 为潮流全 ECS——导致运行时异常或性能回退\n- 只背「ECS、OOP、组件化如何取舍」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 范式、性能 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "233": {
    "answer": "- **结论**：局部回调，全局事件总线，命令用于可记录操作。 事件解耦也降低可追踪。\n- **考察点**：解耦、调试，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：战斗事件通知 UI。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "事件解耦也降低可追踪。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "战斗事件通知 UI。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 事件总线滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只背「事件系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 解耦、调试 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "234": {
    "answer": "- **结论**：导出、校验、生成类型、加载和索引。 配置错误越早发现越好。\n- **考察点**：数据管线，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能表强类型。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "配置错误越早发现越好。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "技能表强类型。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 运行时才校验——导致运行时异常或性能回退\n- 只背「配置表系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 数据管线 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "235": {
    "answer": "- **结论**：版本号、校验、备份、迁移和原子替换。 防止写一半损坏。\n- **考察点**：版本、原子写入，面试时应先给出判断标准再展开。\n- 结合 `EventBus` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：save.tmp 替换。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "防止写一半损坏。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "save.tmp 替换。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接覆盖唯一存档——导致运行时异常或性能回退\n- 只背「存档系统如何保证可靠」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 版本、原子写入 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "236": {
    "answer": "- **结论**：规则放纯 C#，Unity 层只表现。 测试不依赖场景。\n- **考察点**：纯逻辑、确定性，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：CombatWorld 产事件。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "测试不依赖场景。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "CombatWorld 产事件。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 判定写动画事件里——导致运行时异常或性能回退\n- 只背「可测试战斗系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 纯逻辑、确定性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "237": {
    "answer": "- **结论**：少量稳定服务可用，需防重复和清理。 全局状态增加耦合。\n- **考察点**：全局服务，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：Audio、Config。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "全局状态增加耦合。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "Audio、Config。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 到处单例互调——导致运行时异常或性能回退\n- 只背「单例模式在 Unity 中如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 全局服务 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "238": {
    "answer": "- **结论**：Locator 主动查服务，DI 外部注入依赖。 DI 更显式可测。\n- **考察点**：依赖管理，面试时应先给出判断标准再展开。\n- 结合 `DI Container` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：资源、网络服务。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "DI 更显式可测。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "资源、网络服务。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全部静态访问——导致运行时异常或性能回退\n- 只背「Service Locator 和 DI 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 依赖管理 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "239": {
    "answer": "- **结论**：MVP 简单可控，MVVM 适合数据绑定强场景。 目标是视图和业务分离。\n- **考察点**：UI 架构，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：背包 Presenter。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "目标是视图和业务分离。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "背包 Presenter。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 模式套壳——导致运行时异常或性能回退\n- 只背「MVC、MVP、MVVM 在 Unity UI 如何选」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 UI 架构 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "240": {
    "answer": "- **结论**：状态拥有进入、更新、退出，转移条件集中。 限制非法状态。\n- **考察点**：状态、转移，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：角色、流程、UI。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "限制非法状态。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "角色、流程、UI。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- if else 无限堆——导致运行时异常或性能回退\n- 只背「状态机如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 状态、转移 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "241": {
    "answer": "- **结论**：适合 NPC 决策组合，节点表达条件和动作。 树结构可视化和复用。\n- **考察点**：AI、决策，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：怪物 AI。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "树结构可视化和复用。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "怪物 AI。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 简单 AI 过度行为树——导致运行时异常或性能回退\n- 只背「行为树适合什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 AI、决策 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "242": {
    "answer": "- **结论**：配置描述阶段、条件、效果和表现引用。 新技能不改或少改代码。\n- **考察点**：技能阶段、效果，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：前摇命中后摇。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "新技能不改或少改代码。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "前摇命中后摇。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每个技能写类——导致运行时异常或性能回退\n- 只背「技能系统如何数据驱动」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 技能阶段、效果 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "243": {
    "answer": "- **结论**：统一添加、刷新、叠层、到期和效果钩子。 Buff 是状态修改器。\n- **考察点**：生命周期、叠加，面试时应先给出判断标准再展开。\n- 结合 `DI Container` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：中毒、护盾、眩晕。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "Buff 是状态修改器。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "中毒、护盾、眩晕。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Buff 互相直接调用——导致运行时异常或性能回退\n- 只背「Buff 系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 生命周期、叠加 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "244": {
    "answer": "- **结论**：基础值加修饰器，按类型和优先级计算最终值。 便于装备、Buff、成长叠加。\n- **考察点**：数值、修饰器，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：攻击力计算。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "便于装备、Buff、成长叠加。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "攻击力计算。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 到处改最终值——导致运行时异常或性能回退\n- 只背「属性系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 数值、修饰器 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "245": {
    "answer": "- **结论**：任务监听事件更新条件，数据驱动目标。 任务与玩法事件解耦。\n- **考察点**：条件、进度、事件，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：击杀、收集。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "任务与玩法事件解耦。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "击杀、收集。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每个任务硬编码——导致运行时异常或性能回退\n- 只背「任务系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 条件、进度、事件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "246": {
    "answer": "- **结论**：物品配置和实例分离，支持堆叠、锁定、排序。 数据结构影响扩展。\n- **考察点**：物品、堆叠、格子，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：道具、装备。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "数据结构影响扩展。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "道具、装备。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 配置当实例改——导致运行时异常或性能回退\n- 只背「背包系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 物品、堆叠、格子 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "247": {
    "answer": "- **结论**：装备有实例属性和随机词条，穿戴影响属性系统。 装备是属性修饰来源。\n- **考察点**：装备实例、属性，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：RPG 装备。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "装备是属性修饰来源。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "RPG 装备。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只用 itemId 表示装备——导致运行时异常或性能回退\n- 只背「装备系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 装备实例、属性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "248": {
    "answer": "- **结论**：用节点图或表描述台词、条件、选项和动作。 内容与执行器分离。\n- **考察点**：剧情、分支，面试时应先给出判断标准再展开。\n- 结合 `DI Container` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：NPC 对话。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "内容与执行器分离。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "NPC 对话。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 台词写代码——导致运行时异常或性能回退\n- 只背「对话系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 剧情、分支 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "249": {
    "answer": "- **结论**：步骤数据化，条件触发，动作执行，支持跳过和恢复。引导跨系统需中心调度。\n- **考察点**：步骤、条件、遮罩，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：强弱引导。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "引导跨系统需中心调度。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "强弱引导。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 强依赖 UI 层级——导致运行时异常或性能回退\n- 只背「新手引导系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 步骤、条件、遮罩 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "250": {
    "answer": "- **结论**：统计事件驱动，成就条件数据化。成就消费长期行为数据。\n- **考察点**：统计、条件，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：累计击杀。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "成就消费长期行为数据。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "累计击杀。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每成就写代码——导致运行时异常或性能回退\n- 只背「成就系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 统计、条件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "251": {
    "answer": "- **结论**：客户端展示，服务器下单和发货校验。支付必须服务器权威。\n- **考察点**：商品、支付、校验，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：内购、礼包。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "支付必须服务器权威。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "内购、礼包。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端直接发奖——导致运行时异常或性能回退\n- 只背「商城系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 商品、支付、校验 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "252": {
    "answer": "- **结论**：活动入口、时间、条件、奖励数据化，服务器控开关。活动变化频繁。\n- **考察点**：运营、配置，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：节日活动。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "活动变化频繁。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "节日活动。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 发版才能改活动——导致运行时异常或性能回退\n- 只背「活动系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 运营、配置 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "253": {
    "answer": "- **结论**：统一事件名、字段、采样、缓存和上报。数据用于分析和排障。\n- **考察点**：数据、追踪，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：新手漏斗。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "数据用于分析和排障。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "新手漏斗。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 埋点散乱——导致运行时异常或性能回退\n- 只背「日志和埋点系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 数据、追踪 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "254": {
    "answer": "- **结论**：捕获托管、原生和日志上下文，按版本聚合。 崩溃需符号和环境信息。\n- **考察点**：稳定性，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Bugly/Crashlytics。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "崩溃需符号和环境信息。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "Bugly/Crashlytics。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 无符号文件——导致运行时异常或性能回退\n- 只背「崩溃上报系统如何接入」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 稳定性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "255": {
    "answer": "- **结论**：代码热更和资源热更分层，版本协商和回滚。 热更风险在兼容。\n- **考察点**：代码、资源、兼容，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：HybridCLR + Addressables。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "热更风险在兼容。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "HybridCLR + Addressables。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 无灰度无回滚——导致运行时异常或性能回退\n- 只背「热更新架构如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 代码、资源、兼容 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "256": {
    "answer": "- **结论**：模块通过接口注册服务和生命周期。 降低功能接入成本。\n- **考察点**：模块、扩展，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：SDK、活动模块。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "降低功能接入成本。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "SDK、活动模块。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 插件反向依赖主工程——导致运行时异常或性能回退\n- 只背「插件化架构如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 模块、扩展 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "257": {
    "answer": "- **结论**：环境检查、日志、配置、资源、登录、进主场景。 启动是依赖链。\n- **考察点**：Bootstrap、初始化，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：GameRoot 状态机。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "启动是依赖链。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "GameRoot 状态机。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- Manager 无序初始化——导致运行时异常或性能回退\n- 只背「游戏启动流程如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Bootstrap、初始化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "258": {
    "answer": "- **结论**：错误码分域、可读、可定位，并统一弹窗策略。 错误是客户端和服务端契约。\n- **考察点**：网络、业务，面试时应先给出判断标准再展开。\n- 结合 `DI Container` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：登录失败提示。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "错误是客户端和服务端契约。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "登录失败提示。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 到处字符串提示——导致运行时异常或性能回退\n- 只背「错误码体系如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 网络、业务 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "259": {
    "answer": "- **结论**：管理 App 版本、资源版本、协议版本、配置版本。 多版本线上共存。\n- **考察点**：客户端、资源、协议，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：强更和热更。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "多版本线上共存。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "强更和热更。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只用一个版本号——导致运行时异常或性能回退\n- 只背「版本管理策略如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 客户端、资源、协议 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "260": {
    "answer": "- **结论**：高频变化用数据，核心复杂规则用代码抽象。 过度数据化难调试。\n- **考察点**：灵活性、复杂度，面试时应先给出判断标准再展开。\n- 结合 `EventBus` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：技能效果组合。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "过度数据化难调试。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "技能效果组合。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一切表驱动——导致运行时异常或性能回退\n- 只背「数据驱动和代码驱动如何平衡」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 灵活性、复杂度 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "261": {
    "answer": "- **结论**：同步查询用接口，异步通知用事件，操作用命令。 通信方式表达语义。\n- **考察点**：接口、事件、命令，面试时应先给出判断标准再展开。\n- Unity 2023+ 默认 SynchronizationContext 会把 continuation 派发到主线程，但 `Task.Run` 内仍不能直接调 `transform.position`。\n- 销毁对象时 `CancellationTokenSource.Cancel()` 取消 await，避免 `MissingReferenceException`。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- **项目落地**：背包通知 UI。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "通信方式表达语义。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "背包通知 UI。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 模块互相引用——导致运行时异常或性能回退\n- 只背「如何设计模块通信边界」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 接口、事件、命令 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "262": {
    "answer": "- **结论**：从配置、协议、UI 生成代码，手写和生成分离。 减少重复和错误。\n- **考察点**：工具化、强类型，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：Config 类、UI Binder。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "减少重复和错误。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "Config 类、UI Binder。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 手改生成文件——导致运行时异常或性能回退\n- 只背「如何做代码生成」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 工具化、强类型 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "263": {
    "answer": "- **结论**：工具分菜单、窗口、校验器、导入器和批处理。 工具提升团队效率。\n- **考察点**：Editor、工具链，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- **项目落地**：资源检查窗口。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "工具提升团队效率。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "资源检查窗口。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 工具只适合作者——导致运行时异常或性能回退\n- 只背「如何做编辑器工具架构」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Editor、工具链 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "264": {
    "answer": "- **结论**：GM 命令分权限、环境、审计和开关。 调试能力也是风险。\n- **考察点**：调试、线上安全，面试时应先给出判断标准再展开。\n- 结合 `StateMachine` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：测试服 GM 面板。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "调试能力也是风险。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "测试服 GM 面板。 实现时可基于 `StateMachine` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 正式服暴露 GM——导致运行时异常或性能回退\n- 只背「如何做权限和 GM 工具」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 调试、线上安全 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "265": {
    "answer": "- **结论**：用接口封装平台能力，按平台实现。 避免业务散落宏判断。\n- **考察点**：平台抽象，面试时应先给出判断标准再展开。\n- 结合 `EventBus` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：登录、支付、分享。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "避免业务散落宏判断。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "登录、支付、分享。 实现时可基于 `EventBus` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 到处 #if——导致运行时异常或性能回退\n- 只背「如何处理跨平台差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 平台抽象 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "266": {
    "answer": "- **结论**：日志、埋点、性能、崩溃和资源版本统一关联。 线上问题靠证据。\n- **考察点**：日志、指标、追踪，面试时应先给出判断标准再展开。\n- 结合 `ServiceLocator` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：失败上报带上下文。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "线上问题靠证据。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "失败上报带上下文。 实现时可基于 `ServiceLocator` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只在本地可调试——导致运行时异常或性能回退\n- 只背「如何设计可观测性」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 日志、指标、追踪 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "267": {
    "answer": "- **结论**：记录、分级、量化风险并安排偿还窗口。技术债不管理会复利。\n- **考察点**：质量、排期，面试时应先给出判断标准再展开。\n- 结合 `MVVM` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：重构计划。\n- 配置表导出 JSON/Bytes，`ConfigManager.Get<T>(id)` 只读访问。",
    "principle": "技术债不管理会复利。 数据驱动把策划迭代从编译期挪到配置期，但需 Schema 校验与版本迁移。",
    "project": "重构计划。 实现时可基于 `MVVM` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 借口式重构——导致运行时异常或性能回退\n- 只背「如何管理技术债」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 质量、排期 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "268": {
    "answer": "- **结论**：明确目标、约束、备选方案、成本和验证路径。 决策要可解释可回滚。\n- **考察点**：取舍、风险，面试时应先给出判断标准再展开。\n- 结合 `DI Container` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：选热更方案。\n- Gameplay 只依赖 `IInventoryService` 接口，UI/网络/存档各实现 Adapter。",
    "principle": "决策要可解释可回滚。 边界清晰比模式名称重要：变化方向决定依赖方向，稳定层不依赖不稳定层。",
    "project": "选热更方案。 实现时可基于 `DI Container` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 凭喜好选型——导致运行时异常或性能回退\n- 只背「主程如何做技术决策」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 取舍、风险 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "269": {
    "answer": "- **结论**：TCP 可靠有序，UDP 低延迟可自定义可靠。 TCP 队头阻塞影响实时。\n- **考察点**：可靠性、实时性，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：登录 TCP，战斗 UDP。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "TCP 队头阻塞影响实时。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "登录 TCP，战斗 UDP。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 认为 UDP 一定不可靠——导致运行时异常或性能回退\n- 只背「TCP 和 UDP 如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 可靠性、实时性 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "270": {
    "answer": "- **结论**：帧同步同步输入，状态同步同步结果。 确定性和服务器压力不同。\n- **考察点**：同步模型，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：RTS 帧同步，ARPG 状态同步。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "确定性和服务器压力不同。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "RTS 帧同步，ARPG 状态同步。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 浮点不确定——导致运行时异常或性能回退\n- 只背「帧同步和状态同步区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 同步模型 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "271": {
    "answer": "- **结论**：保留 token，重连后拉权威状态或补消息。 断线不等于登出。\n- **考察点**：会话、恢复，面试时应先给出判断标准再展开。\n- 结合 `KCP` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：移动端切后台。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "断线不等于登出。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "移动端切后台。 实现时可基于 `KCP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接回登录——导致运行时异常或性能回退\n- 只背「断线重连如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 会话、恢复 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "272": {
    "answer": "- **结论**：TLS、token、nonce、时间戳、服务器权威。 客户端不可信。\n- **考察点**：加密、签名，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：战斗结算校验。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "客户端不可信。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "战斗结算校验。 实现时可基于 `Protobuf` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端发金币——导致运行时异常或性能回退\n- 只背「客户端网络安全怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 加密、签名 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "273": {
    "answer": "- **结论**：新增字段兼容，破坏性变更分版本或强更。 线上多版本共存。\n- **考察点**：协议演进，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：Protobuf 新 tag。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "线上多版本共存。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "Protobuf 新 tag。 实现时可基于 `WebSocket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 复用 tag——导致运行时异常或性能回退\n- 只背「网络协议如何版本兼容」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 协议演进 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "274": {
    "answer": "- **结论**：Protobuf 小且强类型，JSON 可读灵活。 二进制和文本取舍。\n- **考察点**：序列化，面试时应先给出判断标准再展开。\n- 结合 `HttpClient` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：网络协议用 PB。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "二进制和文本取舍。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "网络协议用 PB。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 配置也盲用 PB——导致运行时异常或性能回退\n- 只背「Protobuf 和 JSON 如何选择」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 序列化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "275": {
    "answer": "- **结论**：定时 ping/pong，超时判弱网或断线。TCP 连接不代表业务可用。\n- **考察点**：连接检测，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：战斗心跳。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "TCP 连接不代表业务可用。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "战斗心跳。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 频率过高耗电——导致运行时异常或性能回退\n- 只背「心跳机制如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 连接检测 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "276": {
    "answer": "- **结论**：TCP 已有序可靠，应用层序号主要用于业务去重与请求-响应配对；UDP/KCP 自定义协议必须自建序号处理乱序、重复与丢包。\n- **TCP 场景**：仍可用自增 seq 做「客户端请求 id」防止重复提交、幂等充值；不必重复造 TCP 传输层轮子。\n- **UDP 场景**：包结构含 `uint32 seq` + `uint32 ack` + payload；接收端维护滑动窗口，`seq <= lastAck` 丢弃重复，`seq > expected` 缓存乱序。\n- **战斗指令**：帧同步/状态同步里序号与服务器帧号绑定，迟到包直接丢弃而非重放，避免「回滚再执行」视觉跳变。\n- **Unity 应用**：`NetworkChannel` 发送前 `Interlocked.Increment(ref _sendSeq)`；接收协程按序 dispatch 到 `MainThreadDispatcher`，超时触发重传或断线重连。",
    "principle": "传输层序号解决「字节流/ datagram 如何可靠有序到达」；应用层序号解决「业务消息语义是否仍有效」。TCP 的 seq 是字节偏移，与应用消息边界无关，因此战斗逻辑仍需 messageId 或 frameId 做去重与过期判定。",
    "project": "KCP 战斗通道：每包 `header.seq` 单调递增，接收端 `Dictionary<uint, Action>` 存未确认 RPC；duplicate seq 直接 ignore，gap 超 3 帧请求服务器重发快照而非逐条补包。",
    "mistakes": "- TCP 上仍按 UDP 逻辑每条等 ack 重传——双重重传导致延迟放大与服务器压力。\n- 序号用 int 溢出未处理——长时间运行后 seq 回绕等于旧包，误当新包执行两次技能。\n- 只递增发送序号不做接收去重——弱网重传导致同一攻击扣血两次，玩家投诉「幽灵伤害」。"
  },
  "277": {
    "answer": "- **结论**：客户端先本地表现，服务器结果回来后校正。用体验换复杂度。\n- **考察点**：延迟隐藏，面试时应先给出判断标准再展开。\n- 结合 `Protobuf` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：移动射击。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "用体验换复杂度。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "移动射击。 实现时可基于 `Protobuf` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 不做回滚校正——导致运行时异常或性能回退\n- 只背「客户端预测是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 延迟隐藏 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "278": {
    "answer": "- **结论**：插值用历史状态平滑，外推预测短期未来。掩盖网络抖动。\n- **考察点**：平滑同步，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：远端玩家移动。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "掩盖网络抖动。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "远端玩家移动。 实现时可基于 `WebSocket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 外推过久穿模——导致运行时异常或性能回退\n- 只背「插值和外推如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 平滑同步 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "279": {
    "answer": "- **结论**：TCP 是字节流，要用长度头或分隔协议拆包。 发送边界不保留。\n- **考察点**：TCP 流，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：长度前缀协议。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "发送边界不保留。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "长度前缀协议。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次 recv 当一包——导致运行时异常或性能回退\n- 只背「网络包粘包拆包如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 TCP 流 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "280": {
    "answer": "- **结论**：关键消息 ack 重传，非关键消息可丢弃。 UDP 不保证可靠和顺序。\n- **考察点**：可靠层，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：位置包可丢，技能包可靠。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "UDP 不保证可靠和顺序。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "位置包可丢，技能包可靠。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有包都重传——导致运行时异常或性能回退\n- 只背「UDP 丢包如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 可靠层 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "281": {
    "answer": "- **结论**：网络线程收包，主线程按预算处理。 避免并发访问 Unity API。\n- **考察点**：主线程派发，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：消息队列限量。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "避免并发访问 Unity API。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "消息队列限量。 实现时可基于 `KCP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 网络线程改 GameObject——导致运行时异常或性能回退\n- 只背「网络消息队列如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 主线程派发 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "282": {
    "answer": "- **结论**：access token 加 refresh token，过期刷新或重登。 避免长期明文凭证。\n- **考察点**：Token、刷新，面试时应先给出判断标准再展开。\n- 结合 `Protobuf` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：自动登录。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "避免长期明文凭证。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "自动登录。 实现时可基于 `Protobuf` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- token 存明文——导致运行时异常或性能回退\n- 只背「登录态如何维护」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Token、刷新 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "283": {
    "answer": "- **结论**：服务端下单、平台回调、客户端查询补单。 客户端结果不可信。\n- **考察点**：订单、补单，面试时应先给出判断标准再展开。\n- 结合 `WebSocket` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：内购掉单恢复。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "客户端结果不可信。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "内购掉单恢复。 实现时可基于 `WebSocket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端直接发货——导致运行时异常或性能回退\n- 只背「支付协议如何保证可靠」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 订单、补单 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "284": {
    "answer": "- **结论**：服务器转发、敏感词、频率限制和历史拉取。聊天是高风险入口。\n- **考察点**：消息、过滤，面试时应先给出判断标准再展开。\n- 结合 `HttpClient` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：世界频道。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "聊天是高风险入口。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "世界频道。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端过滤为主——导致运行时异常或性能回退\n- 只背「聊天系统如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 消息、过滤 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "285": {
    "answer": "- **结论**：展示匹配状态、超时、取消、重连恢复。 匹配状态在服务器。\n- **考察点**：状态、取消，面试时应先给出判断标准再展开。\n- 结合 `Socket` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：PVP 匹配。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "匹配状态在服务器。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "PVP 匹配。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 断线丢状态——导致运行时异常或性能回退\n- 只背「匹配系统客户端关注什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 状态、取消 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "286": {
    "answer": "- **结论**：服务器维护房间权威状态并广播成员变更。房间是共享状态机。\n- **考察点**：房间、成员，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- **项目落地**：组队副本。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "房间是共享状态机。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "组队副本。 实现时可基于 `KCP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端自己改准备状态——导致运行时异常或性能回退\n- 只背「房间系统如何同步状态」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 房间、成员 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "287": {
    "answer": "- **结论**：上传输入或过程数据，服务器校验关键结果。 结果不能只信客户端。\n- **考察点**：校验、回放，面试时应先给出判断标准再展开。\n- 结合 `Protobuf` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：PVE 快速校验。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "结果不能只信客户端。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "PVE 快速校验。 实现时可基于 `Protobuf` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端传胜利——导致运行时异常或性能回退\n- 只背「战斗结算如何防作弊」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 校验、回放 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "288": {
    "answer": "- **结论**：超时提示、重试、缓存请求、降级和重连。移动网络不稳定。\n- **考察点**：超时、重试，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：请求排队。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "移动网络不稳定。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "请求排队。 实现时可基于 `WebSocket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一超时就踢线——导致运行时异常或性能回退\n- 只背「弱网环境如何优化体验」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 超时、重试 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "289": {
    "answer": "- **结论**：HTTP 适合低频业务，长连接适合实时推送。 连接模型和延迟不同。\n- **考察点**：请求模型，面试时应先给出判断标准再展开。\n- 结合 `HttpClient` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：邮件 HTTP，战斗长连。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "连接模型和延迟不同。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "邮件 HTTP，战斗长连。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 所有业务走长连——导致运行时异常或性能回退\n- 只背「HTTP 和长连接如何分工」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 请求模型 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "290": {
    "answer": "- **结论**：适合 H5/小游戏和轻实时业务。 基于 TCP，仍有队头阻塞。\n- **考察点**：长连接、跨平台，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：微信小游戏通信。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "基于 TCP，仍有队头阻塞。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "微信小游戏通信。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 用它做强实时动作——导致运行时异常或性能回退\n- 只背「WebSocket 在游戏中适合什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 长连接、跨平台 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "291": {
    "answer": "- **结论**：通过 TLS 或非对称交换会话密钥。 避免静态密钥泄露。\n- **考察点**：安全、握手，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：登录后会话密钥。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "避免静态密钥泄露。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "登录后会话密钥。 实现时可基于 `KCP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 密钥写客户端——导致运行时异常或性能回退\n- 只背「网络加密密钥如何协商」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 安全、握手 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "292": {
    "answer": "- **结论**：大包或文本包可压缩，小包慎用。压缩省带宽但耗 CPU。\n- **考察点**：带宽、CPU，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：配置下载。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "压缩省带宽但耗 CPU。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "配置下载。 实现时可基于 `Protobuf` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每个小包压缩——导致运行时异常或性能回退\n- 只背「网络压缩什么时候做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 带宽、CPU 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "293": {
    "answer": "- **结论**：非法包记录、丢弃、限流，严重时断开。防止异常数据污染状态。\n- **考察点**：容错、断开，面试时应先给出判断标准再展开。\n- 结合 `WebSocket` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：网关校验。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "防止异常数据污染状态。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "网关校验。 实现时可基于 `WebSocket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端崩溃——导致运行时异常或性能回退\n- 只背「协议错误如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 容错、断开 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "294": {
    "answer": "- **结论**：通过服务器时间和 RTT 估算校正本地时间。 本地时钟不可信。\n- **考察点**：延迟、时间戳，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：活动倒计时。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "本地时钟不可信。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "活动倒计时。 实现时可基于 `HttpClient` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 信任系统时间——导致运行时异常或性能回退\n- 只背「网络时间同步如何做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 延迟、时间戳 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "295": {
    "answer": "- **结论**：记录协议名、错误码、序号、耗时，避免敏感信息。 线上问题要复现链路。\n- **考察点**：排障、隐私，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- **项目落地**：请求失败上报。\n- 弱网模拟：延迟 200ms + 10% 丢包验证重连与 UI 提示。",
    "principle": "线上问题要复现链路。 客户端预测提升手感，但必须以服务器快照校正防止永久分歧。",
    "project": "请求失败上报。 实现时可基于 `Socket` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 打印 token——导致运行时异常或性能回退\n- 只背「网络日志如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 排障、隐私 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "296": {
    "answer": "- **结论**：登录获取区服和网关地址，断线按策略重连或切换。 地址由服务端调度。\n- **考察点**：网关、区服，面试时应先给出判断标准再展开。\n- 结合 `KCP` 在 Editor 中复现并 Profile 验证。\n- **项目落地**：分区服游戏。\n- Charles/mitmproxy 抓包 + 客户端 seq 日志对齐排查乱序/重复。",
    "principle": "地址由服务端调度。 可靠性与实时性不可兼得：TCP 保序增延迟，UDP 低延迟需应用层补可靠性。",
    "project": "分区服游戏。 实现时可基于 `KCP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端写死 IP——导致运行时异常或性能回退\n- 只背「多服务器环境客户端如何切换」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 网关、区服 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "297": {
    "answer": "- **结论**：用 AndroidJavaClass/Object 或插件桥接。 最终通过 JNI 调用。\n- **考察点**：JNI、插件，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- **项目落地**：登录支付广告。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "最终通过 JNI 调用。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "登录支付广告。 实现时可基于 `IL2CPP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 签名不匹配——导致运行时异常或性能回退\n- 只背「Unity 如何调用 Android 原生代码」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 JNI、插件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "298": {
    "answer": "- **结论**：Manifest 声明，危险权限运行时请求并处理结果。 Android 6 后运行时权限。\n- **考察点**：Manifest、运行时，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：相册、录音。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "Android 6 后运行时权限。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "相册、录音。 实现时可基于 `Gradle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只写 Manifest——导致运行时异常或性能回退\n- 只背「Android 权限请求如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Manifest、运行时 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "299": {
    "answer": "- **结论**：用 AAB，裁剪 ABI、资源、Shader 和插件。包体由库和资源组成。\n- **考察点**：AAB、ABI、资源，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- **项目落地**：首包瘦身。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "包体由库和资源组成。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "首包瘦身。 实现时可基于 `JNI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- x86 测试库进包——导致运行时异常或性能回退\n- 只背「Android 包体如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 AAB、ABI、资源 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "300": {
    "answer": "- **结论**：区分 C#、Java、native，保留符号表。 IL2CPP 需符号化。\n- **考察点**：logcat、符号，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：Crashlytics。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "IL2CPP 需符号化。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "Crashlytics。 实现时可基于 `AndroidJavaObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 无 symbols——导致运行时异常或性能回退\n- 只背「Android 崩溃日志如何定位」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 logcat、符号 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "301": {
    "answer": "- **结论**：原生插件需处理 onCreate/onResume/onPause/onDestroy。 Activity 驱动应用生命周期。\n- **考察点**：Activity、回调，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- **项目落地**：SDK 初始化。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "Activity 驱动应用生命周期。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "SDK 初始化。 实现时可基于 `AndroidJavaClass` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 静态保存旧 Activity——导致运行时异常或性能回退\n- 只背「UnityPlayerActivity 生命周期要注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Activity、回调 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "302": {
    "answer": "- **结论**：原生 UI API 必须在 UI 线程调用。Android 视图非线程安全。\n- **考察点**：主线程、runOnUiThread，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：权限弹窗。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "Android 视图非线程安全。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "权限弹窗。 实现时可基于 `IL2CPP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 子线程弹 Toast——导致运行时异常或性能回退\n- 只背「Android UI 线程问题如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 主线程、runOnUiThread 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "303": {
    "answer": "- **结论**：LocalRef 调用期间有效，GlobalRef 可长期持有需释放。 JNI 引用表有限。\n- **考察点**：引用管理，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：缓存 Java 对象。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "JNI 引用表有限。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "缓存 Java 对象。 实现时可基于 `Gradle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- LocalRef 泄漏——Memory Profiler 快照 Diff 可见引用链不断增，切场景内存不降\n- 只背「JNI LocalRef 和 GlobalRef 有何区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 引用管理 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "304": {
    "answer": "- **结论**：多插件 Manifest 会合并，需检查冲突和权限。 Gradle merge 规则处理节点。\n- **考察点**：Manifest、插件，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：多 SDK 接入。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "Gradle merge 规则处理节点。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "多 SDK 接入。 实现时可基于 `JNI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- provider 冲突——导致运行时异常或性能回退\n- 只背「AndroidManifest 合并问题如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Manifest、插件 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "305": {
    "answer": "- **结论**：查看依赖冲突、AGP、SDK、仓库和插件版本。 Android 构建链复杂。\n- **考察点**：依赖、版本，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：SDK 接入。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "Android 构建链复杂。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "SDK 接入。 实现时可基于 `AndroidJavaObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看最后一行错误——导致运行时异常或性能回退\n- 只背「Gradle 构建失败如何排查」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 依赖、版本 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "306": {
    "answer": "- **结论**：AAR 包含资源和 Manifest，JAR 只有 class。 Android 资源需 AAR。\n- **考察点**：Android 库，面试时应先给出判断标准再展开。\n- `Vector3`/`Quaternion` 是 struct，作为字段赋值会拷贝 12~16 字节；`MonoBehaviour` 必须是 class 才能挂到 GameObject。\n- Profiler 中对比 struct 传参与 class 引用：大 struct（>16 字节）按值传递可能比一次 GC 更贵。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- **项目落地**：SDK 插件。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "Android 资源需 AAR。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "SDK 插件。 实现时可基于 `AndroidJavaClass` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 把资源库做 JAR——导致运行时异常或性能回退\n- 只背「AAR 和 JAR 区别」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Android 库 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "307": {
    "answer": "- **结论**：Android 发布包选 IL2CPP 获更好性能与安全性；Mono 适合 Editor/快速迭代。IL2CPP 需处理 AOT 裁剪、反射与泛型限制。\n- **IL2CPP 流程**：C# → IL → C++ → NDK 编译 native .so；启动时无 JIT（除可选 HybridCLR 等扩展），方法调用为直接函数指针。\n- **与 Mono 差异**：Mono 用 JIT 解释/编译 IL，包体较小、构建快，但运行时性能与代码保护弱于 native；Google Play 64 位要求使 IL2CPP 成为主流。\n- **AOT 坑**：`link.xml` 保留反射用到的类型；泛型 `JsonUtility.FromJson<T>` 若 T 未在 AOT 实例化列表会 Runtime 崩溃。\n- **Unity 应用**：Player Settings → Scripting Backend = IL2CPP，Target Architectures 勾 ARM64；`Managed Stripping Level=Low/Medium` 后跑一遍全量功能测试 + `dotnet` 符号表上传 Bugly。",
    "principle": "IL2CPP 将托管代码静态编译为平台 native 指令，消除 JIT 开销并提高逆向门槛；代价是构建链更长、部分动态特性（Emit、某些反射）需在编译期可解析。Unity 的 Managed Stripping 在 IL2CPP 前移除未引用类型，进一步要求 link.xml 显式保留 JNI/序列化/热更入口。",
    "project": "RPG Android 包：IL2CPP + ARM64，JNI 回调 `AndroidJavaObject` 支付 SDK；曾因 `link.xml` 未保留 `GooglePlayGames.XXX` 导致 Release 闪退，Development 包 Mono 正常——典型 Stripping 问题。",
    "mistakes": "- Release IL2CPP 未测反射注册路径——线上首启 Json 反序列化 MissingMethodException。\n- 只打 ARMv7 未开 ARM64——Play Store 拒审或高端机性能差。\n- 以为 IL2CPP 自动更快不优化 GC/逻辑——CPU Profiler 仍可能主线程脚本热点，native 只是执行更快。"
  },
  "308": {
    "answer": "- **结论**：发布需包含 arm64-v8a，按渠道要求保留 ABI。 平台商店要求 64 位。\n- **考察点**：ABI、arm64，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：Google Play。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "平台商店要求 64 位。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "Google Play。 实现时可基于 `Gradle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只打 armv7——导致运行时异常或性能回退\n- 只背「Android 64 位要求如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ABI、arm64 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "309": {
    "answer": "- **结论**：应用私有目录无需权限，公共媒体需系统 API。 新版 Android 限制外部存储。\n- **考察点**：沙盒、权限，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：下载资源、截图。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "新版 Android 限制外部存储。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "下载资源、截图。 实现时可基于 `JNI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接写根目录——导致运行时异常或性能回退\n- 只背「Android 存储路径怎么选」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 沙盒、权限 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "310": {
    "answer": "- **结论**：Android 13 起通知需运行时授权。通知变危险权限。\n- **考察点**：Android 13、通知，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：推送通知。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "通知变危险权限。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "推送通知。 实现时可基于 `AndroidJavaObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 默认认为可通知——导致运行时异常或性能回退\n- 只背「Android 通知权限如何处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Android 13、通知 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "311": {
    "answer": "- **结论**：暂停渲染、音频、网络策略，保存状态。 后台可能被系统杀进程。\n- **考察点**：生命周期、资源，面试时应先给出判断标准再展开。\n- 暂停菜单 DOTween：`tween.SetUpdate(true)` 使用 unscaled time。\n- `Animator.updateMode = AnimatorUpdateMode.UnscaledTime` 让 UI 角色在 timeScale=0 时仍播放。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- **项目落地**：切后台保存。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "后台可能被系统杀进程。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "切后台保存。 实现时可基于 `AndroidJavaClass` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 后台继续满帧——导致运行时异常或性能回退\n- 只背「Android 后台和前台切换注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 生命周期、资源 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "312": {
    "answer": "- **结论**：可用 UnitySendMessage 或 C# JNI 轮询/回调桥。 UnitySendMessage 通过对象名字符串。\n- **考察点**：UnitySendMessage，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：SDK 登录回调。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "UnitySendMessage 通过对象名字符串。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "SDK 登录回调。 实现时可基于 `IL2CPP` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 对象名改了失效——导致运行时异常或性能回退\n- 只背「Android 原生回调到 Unity 怎么做」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 UnitySendMessage 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "313": {
    "answer": "- **结论**：原生 Java 代码可混淆，需 keep SDK 和反射类。 混淆可能改名破坏反射。\n- **考察点**：ProGuard/R8，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：SDK keep rules。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "混淆可能改名破坏反射。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "SDK keep rules。 实现时可基于 `Gradle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 混淆后崩溃——导致运行时异常或性能回退\n- 只背「Android 混淆和 Unity 关系」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 ProGuard/R8 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "314": {
    "answer": "- **结论**：查看 traces，定位主线程耗时 IO、锁或初始化。 主线程长时间无响应触发 ANR。\n- **考察点**：主线程阻塞，面试时应先给出判断标准再展开。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- IL2CPP Stripping 后 JNI 反射类名需在 `link.xml` `<preserve>` 保留。\n- **项目落地**：启动卡死。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "主线程长时间无响应触发 ANR。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "启动卡死。 实现时可基于 `JNI` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- onCreate 做重活——导致运行时异常或性能回退\n- 只背「Android ANR 如何定位」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 主线程阻塞 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "315": {
    "answer": "- **结论**：注意生命周期、JS Bridge 安全、权限和线程。 WebView 是原生复杂组件。\n- **考察点**：Web、桥接，面试时应先给出判断标准再展开。\n- Script Execution Order 设为负数可让 InputManager 先于 PlayerController.Awake 执行。\n- 对象池 `SetActive(false)` 触发 OnDisable 但不 Destroy，再次启用时 OnEnable 会重跑。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- **项目落地**：公告、客服。\n- Gradle `mainTemplate.gradle` 统一 SDK 与 NDK 版本。",
    "principle": "WebView 是原生复杂组件。 IL2CPP AOT 在构建期确定泛型实例，运行时无法 JIT 新组合。",
    "project": "公告、客服。 实现时可基于 `AndroidJavaObject` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- JS 接口暴露危险——导致运行时异常或性能回退\n- 只背「Android WebView 接入注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Web、桥接 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "316": {
    "answer": "- **结论**：用统一接口封装登录、支付、广告，不同渠道实现。 平台差异集中。\n- **考察点**：多渠道、抽象，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- `new AndroidJavaClass(\"com.unity3d.player.UnityPlayer\").GetStatic<AndroidJavaObject>(\"currentActivity\")` 取 Activity。\n- **项目落地**：国内多渠道包。\n- logcat `-s Unity` 过滤；Release IL2CPP 必跑全量回归。",
    "principle": "平台差异集中。 JNI 每次调用有固定开销，批量传递 primitive array 比逐字段 Get 更省。",
    "project": "国内多渠道包。 实现时可基于 `AndroidJavaClass` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 业务直接调某 SDK——导致运行时异常或性能回退\n- 只背「Android 渠道 SDK 如何架构」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 多渠道、抽象 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "317": {
    "answer": "- **结论**：受首包、内存、文件系统、合法域名和平台 API 限制。 运行在微信容器非原生 App。\n- **考察点**：WebGL、包体，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：首包只放启动资源。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "运行在微信容器非原生 App。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "首包只放启动资源。 实现时可基于 `AssetBundle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 按 Android 思路发布——导致运行时异常或性能回退\n- 只背「Unity 发布微信小游戏主要限制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 WebGL、包体 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "318": {
    "answer": "- **结论**：首包最小化，分包和 CDN 按场景下载缓存。 缓存空间和网络受限。\n- **考察点**：首包、CDN、缓存，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：章节资源远程。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "缓存空间和网络受限。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "章节资源远程。 实现时可基于 `分包加载` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 全资源首包——导致运行时异常或性能回退\n- 只背「小游戏资源加载如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 首包、CDN、缓存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "319": {
    "answer": "- **结论**：WebGL 更怕内存峰值、JS Bridge 和加载阻塞。 受 JS 引擎和容器影响。\n- **考察点**：WebGL、内存，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：降低粒子和后处理。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "受 JS 引擎和容器影响。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "降低粒子和后处理。 实现时可基于 `WebGL` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 套高端安卓画质——导致运行时异常或性能回退\n- 只背「小游戏性能和原生 App 差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 WebGL、内存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "320": {
    "answer": "- **结论**：通过 JS API 桥接，登录和支付由服务端校验。 平台能力在 JS 环境。\n- **考察点**：平台 API、安全，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：code 换 session。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "平台能力在 JS 环境。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "code 换 session。 实现时可基于 `WXSDK` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端信任支付结果——导致运行时异常或性能回退\n- 只背「微信登录分享支付如何接入」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 平台 API、安全 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "321": {
    "answer": "- **结论**：裁资源、远程化、压缩、删无用代码和 Shader。 首包影响审核和启动。\n- **考察点**：包体、裁剪，面试时应先给出判断标准再展开。\n- Frame Debugger 看 `RenderLoop.Draw` 批次：SRP Batcher 合并条件是同一 shader variant + 兼容 CBUFFER。\n- `MaterialPropertyBlock` 改色不 `new Material()`，避免破坏合批。\n- **项目落地**：新手场景首包。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "首包影响审核和启动。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "新手场景首包。 实现时可基于 `Wasm` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 测试资源进包——导致运行时异常或性能回退\n- 只背「首包体积如何控制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 包体、裁剪 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "322": {
    "answer": "- **结论**：按玩法或章节划分，进入前下载并缓存。 分包减少首包压力。\n- **考察点**：分包、加载，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：主城、战斗分包。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "分包减少首包压力。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "主城、战斗分包。 实现时可基于 `AssetBundle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 分包间互相强依赖——导致运行时异常或性能回退\n- 只背「小游戏分包如何设计」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 分包、加载 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "323": {
    "answer": "- **结论**：文件名或 manifest 带 hash，更新时换版本。 CDN 和本地缓存可能旧。\n- **考察点**：版本、缓存，面试时应先给出判断标准再展开。\n- 内部 `Entry[]` + 桶数组，负载因子超阈值时扩容为约 2 倍并重算 hash 索引。\n- 自定义 key 的 `GetHashCode()` 必须稳定：CombatEntity 作 key 时若 hash 随状态变会导致查不到。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：v123/asset.hash。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "CDN 和本地缓存可能旧。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "v123/asset.hash。 实现时可基于 `分包加载` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 覆盖同名文件——导致运行时异常或性能回退\n- 只背「小游戏 CDN 资源版本如何管理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 版本、缓存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "324": {
    "answer": "- **结论**：只缓存必要资源，定期清理和检测空间。可用空间有限。\n- **考察点**：本地缓存，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：清理旧章节。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "可用空间有限。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "清理旧章节。 实现时可基于 `WebGL` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 永不清缓存——导致运行时异常或性能回退\n- 只背「小游戏文件系统限制怎么处理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 本地缓存 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "325": {
    "answer": "- **结论**：请求、下载、WebSocket 需配置合法域名。 平台安全限制。\n- **考察点**：域名、HTTPS，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：CDN 域名配置。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "平台安全限制。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "CDN 域名配置。 实现时可基于 `WXSDK` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 本地测试域名上线失败——导致运行时异常或性能回退\n- 只背「微信小游戏网络合法域名影响」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 域名、HTTPS 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "326": {
    "answer": "- **结论**：减少高频互调，批量传输数据。 C# 与 JS 互调有开销。\n- **考察点**：跨语言调用，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：埋点批量上报。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "C# 与 JS 互调有开销。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "埋点批量上报。 实现时可基于 `Wasm` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 每帧调用 JS——导致运行时异常或性能回退\n- 只背「小游戏 JS Bridge 成本如何优化」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 跨语言调用 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "327": {
    "answer": "- **结论**：看内存峰值、资源加载、纹理尺寸和日志。 超内存可能被容器杀。\n- **考察点**：内存、WebGL，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- **项目落地**：大场景进场。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "超内存可能被容器杀。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "大场景进场。 实现时可基于 `AssetBundle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次解压大包——导致运行时异常或性能回退\n- 只背「小游戏内存白屏如何排查」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 内存、WebGL 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "328": {
    "answer": "- **结论**：部分平台音频需用户交互后播放，注意格式和缓存。 浏览器环境有自动播放限制。\n- **考察点**：用户手势、兼容，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：BGM 首次点击后播放。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "浏览器环境有自动播放限制。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "BGM 首次点击后播放。 实现时可基于 `分包加载` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 启动自动播失败——导致运行时异常或性能回退\n- 只背「小游戏音频播放限制」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 用户手势、兼容 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "329": {
    "answer": "- **结论**：好友排行榜用开放数据域，主域与开放域通信。隔离用户隐私数据。\n- **考察点**：开放数据域，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：好友分数。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "隔离用户隐私数据。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "好友分数。 实现时可基于 `WebGL` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 主域直接读好友信息——导致运行时异常或性能回退\n- 只背「小游戏排行榜如何实现」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 开放数据域 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "330": {
    "answer": "- **结论**：处理加载失败、关闭回调、频控和服务器发奖。 广告结果要校验。\n- **考察点**：激励视频、插屏，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：看广告领奖。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "广告结果要校验。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "看广告领奖。 实现时可基于 `WXSDK` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 客户端直接加奖励——导致运行时异常或性能回退\n- 只背「小游戏广告接入注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 激励视频、插屏 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "331": {
    "answer": "- **结论**：注意隐私协议、支付、诱导分享、资源域名和内容合规。 平台审核规则严格。\n- **考察点**：合规、隐私，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：上线前清单。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "平台审核规则严格。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "上线前清单。 实现时可基于 `Wasm` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 强制分享领奖——导致运行时异常或性能回退\n- 只背「小游戏审核常见问题」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 合规、隐私 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "332": {
    "answer": "- **结论**：资源可更新，代码和 wasm 更新受平台包版本限制。 小游戏包需平台审核。\n- **考察点**：平台限制，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：资源 CDN 更新。\n- 真机双端测首包加载、内存 warning、支付/分享完整链路。",
    "principle": "小游戏包需平台审核。 Wasm 线性内存上限远低于原生 App，纹理/AudioClip 必须分档与流式。",
    "project": "资源 CDN 更新。 实现时可基于 `AssetBundle` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 期望随意热更代码——导致运行时异常或性能回退\n- 只背「小游戏热更新和 App 热更差异」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 平台限制 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "333": {
    "answer": "- **结论**：开发者工具看日志和性能，最终必须真机验证。 工具环境与真机不同。\n- **考察点**：DevTools、真机，面试时应先给出判断标准再展开。\n- 首包 Wasm + 分包 AB：超过 4MB 首包需 CDN 远程包，`wx.downloadFile` 失败要有重试与降级 UI。\n- `wx.onMemoryWarning` 回调里 `Resources.UnloadUnusedAssets` + 释放非必要 Addressables。\n- **项目落地**：网络和内存调试。\n- 基础库最低版本用 `canIUse` 做 API 降级分支。",
    "principle": "工具环境与真机不同。 宿主 WebView 生命周期与 Unity Pause 不同步，需监听 `wx.onShow/onHide`。",
    "project": "网络和内存调试。 实现时可基于 `分包加载` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只在工具通过——导致运行时异常或性能回退\n- 只背「小游戏调试工具如何使用」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 DevTools、真机 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "334": {
    "answer": "- **结论**：微信小游戏需建立 iOS/Android × 性能档位 × 基础库版本 的适配矩阵，覆盖安全区、输入、内存与 API 差异。\n- **安全区**：`wx.getSystemInfoSync().safeArea` 驱动 UGUI 顶栏/底栏 padding；iPhone 刘海与 Android 打孔屏 inset 不同，需 Canvas Scaler + SafeAreaFitter 组件。\n- **性能档位**：低端机关闭后处理、降粒子、限制同屏 Draw Call；用 `wx.getDeviceBenchmarkInfo` 或自研帧率/内存探针分 Low/Mid/High。\n- **基础库版本**：支付、开放数据域、Worker 等 API 有最低版本；`wx.canIUse` 做降级，QA 矩阵含 2.30+ / 3.0+ 代表版本。\n- **Unity 应用**：WebGL/Wasm 包体分包 + 首包资源压缩；Android 微信与 iOS 微信在音频自动播放、触摸延迟上行为不同，需真机双端各测一遍完整新手流程。",
    "principle": "微信小游戏运行在宿主 WebView + 自定义 Wasm 运行时中，设备碎片化比原生 App 更严重：同一 Unity 构建在 iOS 微信的 JIT/Wasm 限制与 Android 微信的内存回收策略可能完全不同。基础库版本决定 JS API 能力边界，而非仅 OS 版本。",
    "project": "卡牌小游戏：适配矩阵 Excel 记录 20 款机型 × 微信 8.0.4x/8.0.5x；iOS 低端机内存 1.2GB 触发 Kill，通过纹理 Max Size 512 + ASTC 6×6 压到 400MB 以下才稳定。",
    "mistakes": "- 只测开发者自己的 flagship Android——iOS 微信首包 Wasm 编译慢 30s，被误判为「卡死」未做 loading 拆分。\n- 忽略 safeArea 导致购买按钮被 Home Indicator 遮挡——转化率下降难复现（QA 用无刘海模拟器）。\n- 调用新 API 未 `canIUse`——旧基础库用户黑屏，后台错误率 spike 但本地 Debug 正常。"
  },
  "335": {
    "answer": "- **结论**：给清晰上下文和约束，生成后必须人工审查、测试和性能验证。 AI 输出是候选方案，不等于已验证代码。\n- **考察点**：Review、测试、上下文，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：生成 UI 模板和编辑器工具。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "AI 输出是候选方案，不等于已验证代码。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "生成 UI 模板和编辑器工具。 实现时可基于 `Cursor` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 直接复制核心代码——导致运行时异常或性能回退\n- 只背「AI 生成代码如何保证质量」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Review、测试、上下文 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "336": {
    "answer": "- **结论**：AI 可分析采样、日志和调用栈，但必须真机验证。 性能问题依赖运行时数据。\n- **考察点**：Profiler、证据链，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：分析 GC Alloc 和 Timeline。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "性能问题依赖运行时数据。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "分析 GC Alloc 和 Timeline。 实现时可基于 `Copilot` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 让 AI 猜瓶颈——导致运行时异常或性能回退\n- 只背「AI 辅助性能优化如何落地」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Profiler、证据链 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "337": {
    "answer": "- **结论**：适合为纯 C# 逻辑生成边界、Mock 和回归用例。 输入输出清晰时质量更高。\n- **考察点**：单测、边界、回归，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：伤害公式、背包合并。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "输入输出清晰时质量更高。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "伤害公式、背包合并。 实现时可基于 `Code Review` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只生成 happy path——导致运行时异常或性能回退\n- 只背「AI 生成测试用例有什么价值」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 单测、边界、回归 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "338": {
    "answer": "- **结论**：整理架构文档、README、事故复盘并接入检索问答。 回答质量取决于知识源。\n- **考察点**：文档、检索、可信源，面试时应先给出判断标准再展开。\n- `IEventBus.Publish<DamageEvent>` 让 UI 血条与战斗解耦，新系统只订阅不修改 PlayerHealth 源码。\n- 有限状态机 `enum State { Idle, Attack }` + `Dictionary<State,IState>` 避免 bool 组合爆炸。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- **项目落地**：新人问资源系统流程。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "回答质量取决于知识源。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "新人问资源系统流程。 实现时可基于 `Prompt` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 文档过期仍引用——导致运行时异常或性能回退\n- 只背「AI 知识库如何建设」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 文档、检索、可信源 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "339": {
    "answer": "- **结论**：可辅助发现空引用、资源泄漏、异常路径和风格问题。 AI 能读上下文但不能替代负责人。\n- **考察点**：静态检查、风险提示，面试时应先给出判断标准再展开。\n- Memory Profiler 拍两次快照 Diff：查 `Texture2D`、`Mesh`、`AssetBundle` 引用链谁持有 handle。\n- 静态事件 `OnPlayerDead += Handler` 未 `-=` 会导致 Scene 卸载后对象仍被引用无法 GC。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- **项目落地**：PR 预审摘要。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "AI 能读上下文但不能替代负责人。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "PR 预审摘要。 实现时可基于 `Roslyn` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 把 AI 当最终审批——导致运行时异常或性能回退\n- 只背「AI 代码 Review 能做什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 静态检查、风险提示 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "340": {
    "answer": "- **结论**：先让 AI 分析影响面，再分小步改造并跑测试。 重构风险来自行为变化。\n- **考察点**：依赖分析、小步验证，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：拆分臃肿 Manager。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "重构风险来自行为变化。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "拆分臃肿 Manager。 实现时可基于 `Cursor` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 一次性大改——导致运行时异常或性能回退\n- 只背「AI 如何辅助重构」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 依赖分析、小步验证 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "341": {
    "answer": "- **结论**：让 AI 根据配置规范生成校验脚本和异常样例。 配置错误应在构建前发现。\n- **考察点**：规则、工具化，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：技能表字段校验。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "配置错误应在构建前发现。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "技能表字段校验。 实现时可基于 `Copilot` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只靠运行时报错——导致运行时异常或性能回退\n- 只背「AI 如何辅助配置表校验」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 规则、工具化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "342": {
    "answer": "- **结论**：可生成窗口、菜单、批处理和导入器雏形。 工具代码模式固定、收益高。\n- **考察点**：Editor、自动化，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：资源命名检查工具。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "工具代码模式固定、收益高。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "资源命名检查工具。 实现时可基于 `Code Review` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 工具无异常处理——导致运行时异常或性能回退\n- 只背「AI 如何辅助编辑器工具开发」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 Editor、自动化 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "343": {
    "answer": "- **结论**：可分析资源清单、重复引用、命名问题和分组建议。 资源问题多为规则和统计问题。\n- **考察点**：依赖、命名、冗余，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：Addressables Analyze 报告解释。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "资源问题多为规则和统计问题。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "Addressables Analyze 报告解释。 实现时可基于 `Prompt` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 不验证打包结果——导致运行时异常或性能回退\n- 只背「AI 如何辅助资源管理」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 依赖、命名、冗余 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "344": {
    "answer": "- **结论**：可生成协议模型、Mock 数据和兼容测试，但协议设计需人工把关。 协议是客户端服务端契约。\n- **考察点**：协议、兼容、测试，面试时应先给出判断标准再展开。\n- TCP Nagle 与粘包：应用层用长度头 `uint16 len + payload` 拆包。\n- 状态同步：客户端发输入 seq，服务器广播帧号；迟到包 seq < serverFrame 直接丢弃。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- **项目落地**：Protobuf 字段变更检查。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "协议是客户端服务端契约。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "Protobuf 字段变更检查。 实现时可基于 `Roslyn` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 复用旧 tag——导致运行时异常或性能回退\n- 只背「AI 如何辅助网络协议开发」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 协议、兼容、测试 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "345": {
    "answer": "- **结论**：把日志、版本、设备和复现步骤提供给 AI，让其给假设和验证顺序。 AI 擅长归纳线索。\n- **考察点**：日志、调用栈、复现，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：崩溃堆栈分类。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "AI 擅长归纳线索。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "崩溃堆栈分类。 实现时可基于 `Cursor` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只给一句现象——导致运行时异常或性能回退\n- 只背「AI 如何辅助 Bug 定位」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 日志、调用栈、复现 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "346": {
    "answer": "- **结论**：限制核心模块自动修改，要求 Review、测试、记录和回滚。 AI 引入的是生产力也引入风险。\n- **考察点**：权限、审计、规范，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：生成代码必须走 PR。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "AI 引入的是生产力也引入风险。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "生成代码必须走 PR。 实现时可基于 `Copilot` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 私自提交 AI 大改——导致运行时异常或性能回退\n- 只背「AI 在团队流程中如何控风险」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 权限、审计、规范 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "347": {
    "answer": "- **结论**：不直接使用来源不明素材和敏感代码，避免上传私密数据。 数据和版权风险不可忽视。\n- **考察点**：合规、安全，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：内部代码脱敏后提问。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "数据和版权风险不可忽视。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "内部代码脱敏后提问。 实现时可基于 `Code Review` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 上传密钥和日志 token——导致运行时异常或性能回退\n- 只背「AI 生成内容版权和安全要注意什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 合规、安全 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "348": {
    "answer": "- **结论**：用交付时间、缺陷率、测试覆盖、Review 时间等指标评估。 不能只凭主观感觉判断收益。\n- **考察点**：效率、质量、指标，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：工具开发提效统计。\n- AI 产出走 PR + Roslyn 分析器 + 人工 Play Mode 验证清单。",
    "principle": "不能只凭主观感觉判断收益。 LLM 训练数据滞后于 Unity LTS 版本，生成代码可能调用已废弃 API。",
    "project": "工具开发提效统计。 实现时可基于 `Prompt` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只看生成行数——导致运行时异常或性能回退\n- 只背「如何评价 AI 工具收益」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 效率、质量、指标 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "349": {
    "answer": "- **结论**：核心是需求理解、系统设计、工程验证和跨团队落地。 AI 降低编码门槛但不替代决策。\n- **考察点**：判断力、架构、验证，面试时应先给出判断标准再展开。\n- AI 生成 MonoBehaviour 常漏 `OnDestroy` Release 与 `OnDisable` 退订；合入前必跑 Play Mode 进出 3 次。\n- Prompt 应包含：Unity 版本、URP/Built-in、生命周期约束、禁止 async void。\n- **项目落地**：主程用 AI 扩展产能。\n- 团队 Prompt 模板库沉淀 Unity 项目约束，减少幻觉 API。",
    "principle": "AI 降低编码门槛但不替代决策。 AI 适合样板与工具，架构决策与性能关键路径仍需工程师负责。",
    "project": "主程用 AI 扩展产能。 实现时可基于 `Roslyn` 做最小验证场景，在 Play Mode 用 Profiler 记录优化/改造前后数据，便于面试追问举证。",
    "mistakes": "- 只会复制答案——导致运行时异常或性能回退\n- 只背「AI 时代 Unity 工程师核心竞争力是什么」概念不在 Unity Editor 实测——追问细节（类名、API、Profile 数据）时露馅。\n- 忽视 判断力、架构、验证 相关边界（对象销毁、切场景、域重载、弱网）——集成阶段才暴露。"
  },
  "350": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 战斗、资源、UI 框架。 中，我负责 一个你主导的核心系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 一个你主导的核心系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。按 STAR 结构组织：Situation（项目背景与约束）→ Task（你的目标与职责）→ Action（技术方案、难点与取舍）→ Result（量化结果与复盘）。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。面试看取舍能力。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。面试看取舍能力。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「一个你主导的核心系统」匹配的真实案例：战斗、资源、UI 框架。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只罗列功能——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "351": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Catalog 配错回滚。 中，我负责 处理线上严重 Bug。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 处理线上严重 Bug 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。先止血，再定位、修复、验证、灰度和复盘。 线上稳定性优先。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。线上稳定性优先。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。线上稳定性优先。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「处理线上严重 Bug」匹配的真实案例：Catalog 配错回滚。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只讲修代码——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "352": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 首帧 350ms 降到 80ms。 中，我负责 推动性能优化落地。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 推动性能优化落地 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。给优化前后指标、定位过程、方案和回归机制。 优化要用数据证明。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。优化要用数据证明。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。优化要用数据证明。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「推动性能优化落地」匹配的真实案例：首帧 350ms 降到 80ms。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 没有设备型号——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "353": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 manifest 加 hash。 中，我负责 资源热更新方案。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 资源热更新方案 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。说明打包、版本、下载、校验、加载、失败重试和回滚。 热更链路长风险高。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。热更链路长风险高。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。热更链路长风险高。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「资源热更新方案」匹配的真实案例：manifest 加 hash。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 无灰度无校验——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "354": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 技能阶段配置。 中，我负责 战斗系统设计。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 战斗系统设计 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。技能数据驱动，Buff 生命周期统一，伤害纯逻辑，表现订阅事件。 战斗变化频繁需解耦。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。战斗变化频繁需解耦。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。战斗变化频繁需解耦。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「战斗系统设计」匹配的真实案例：技能阶段配置。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 判定写死动画里——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "355": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Popup、Toast、HUD 分层。 中，我负责 UI 框架设计。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 UI 框架设计 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。讲页面生命周期、层级、返回栈、异步加载和释放策略。 UI 是高频业务入口。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。UI 是高频业务入口。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。UI 是高频业务入口。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「UI 框架设计」匹配的真实案例：Popup、Toast、HUD 分层。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 页面互相 Find——O(n) 遍历 + 字符串比较，切场景后可能找到错误实例\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "356": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 道具和装备共存。 中，我负责 背包系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 背包系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。配置和实例分离，支持堆叠、装备唯一属性、排序和筛选。 物品既有静态配置也有动态实例。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。物品既有静态配置也有动态实例。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。物品既有静态配置也有动态实例。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「背包系统」匹配的真实案例：道具和装备共存。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只用 itemId——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "357": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 击杀、收集、对话任务。 中，我负责 任务系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 任务系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。任务监听玩法事件，条件和奖励配置化。 任务是事件消费方。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。任务是事件消费方。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。任务是事件消费方。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「任务系统」匹配的真实案例：击杀、收集、对话任务。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 每个任务硬编码——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "358": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 强引导点击按钮。 中，我负责 新手引导系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 新手引导系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。步骤数据化，支持条件触发、遮罩、跳过、断点恢复。 引导跨多个系统。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。引导跨多个系统。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。引导跨多个系统。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「新手引导系统」匹配的真实案例：强引导点击按钮。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 绑定脆弱路径——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "359": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 ARPG 主角移动。 中，我负责 角色控制器。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 角色控制器 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。输入采样、移动解算、碰撞、动画参数和状态分离。 手感来自多系统协同。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。手感来自多系统协同。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。手感来自多系统协同。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「角色控制器」匹配的真实案例：ARPG 主角移动。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- Transform 物理混用——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "360": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 巡逻追击攻击。 中，我负责 怪物 AI。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 怪物 AI 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。感知、黑板、决策树或状态机、行为执行分离。 AI 复杂度来自决策组合。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。AI 复杂度来自决策组合。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。AI 复杂度来自决策组合。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「怪物 AI」匹配的真实案例：巡逻追击攻击。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 所有逻辑写 Update——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "361": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 技能时间轴工具。 中，我负责 技能编辑器。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 技能编辑器 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。编辑器编辑时间轴、效果、判定和预览，导出配置给运行时。 工具降低内容生产成本。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。工具降低内容生产成本。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。工具降低内容生产成本。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「技能编辑器」匹配的真实案例：技能时间轴工具。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 工具与运行时不一致——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "362": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 SkillConfig 生成。 中，我负责 配置表管线。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 配置表管线 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。Excel 到 JSON/二进制，构建前校验并生成强类型代码。 配置错误越早发现越好。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。配置错误越早发现越好。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。配置错误越早发现越好。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「配置表管线」匹配的真实案例：SkillConfig 生成。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 运行时才报错——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "363": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 save.tmp 替换。 中，我负责 存档系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 存档系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。版本号、迁移、校验、备份、原子写入和云同步。 存档最重要是可恢复。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。存档最重要是可恢复。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。存档最重要是可恢复。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「存档系统」匹配的真实案例：save.tmp 替换。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 直接覆盖存档——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "364": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 PVP 状态同步。 中，我负责 网络同步方案。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 网络同步方案 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。按玩法说明同步模型、延迟处理、重连和反作弊。 同步方案受玩法约束。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。同步方案受玩法约束。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。同步方案受玩法约束。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「网络同步方案」匹配的真实案例：PVP 状态同步。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只讲协议不讲体验——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "365": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 内购补单。 中，我负责 支付系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 支付系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。客户端展示和发起，服务器下单、回调、补单和发货。支付结果不能信客户端。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。支付结果不能信客户端。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。支付结果不能信客户端。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「支付系统」匹配的真实案例：内购补单。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 客户端直接加货币——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "366": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 限时礼包。 中，我负责 商城活动系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 商城活动系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。商品、活动、时间、条件、奖励由服务器或配置控制。运营需求变化频繁。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。运营需求变化频繁。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。运营需求变化频繁。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「商城活动系统」匹配的真实案例：限时礼包。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 发版改活动——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "367": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 TMP fallback。 中，我负责 多语言系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 多语言系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。文案 key 化，支持参数、复数、字体 fallback 和资源差异。 本地化不只是翻译文本。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。本地化不只是翻译文本。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。本地化不只是翻译文本。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「多语言系统」匹配的真实案例：TMP fallback。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 文案写死代码——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "368": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 战斗音效池。 中，我负责 音频系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 音频系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。管理音频分组、优先级、淡入淡出、对象池和音量设置。 音频资源也有生命周期。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。音频资源也有生命周期。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。音频资源也有生命周期。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「音频系统」匹配的真实案例：战斗音效池。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 每次 new AudioSource——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "369": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 技能特效预热。 中，我负责 特效系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 特效系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。特效按生命周期池化，支持挂点、缩放、裁剪和画质档位。 特效容易带来 GPU 和内存压力。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。特效容易带来 GPU 和内存压力。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。特效容易带来 GPU 和内存压力。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「特效系统」匹配的真实案例：技能特效预热。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 播完不回收——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "370": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Cinemachine。 中，我负责 相机系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 相机系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。相机状态机处理跟随、锁定、震屏、遮挡和过渡。 相机影响手感和观感。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。相机影响手感和观感。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。相机影响手感和观感。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「相机系统」匹配的真实案例：Cinemachine。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 逻辑散落各处——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "371": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 新 Input System。 中，我负责 输入系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 输入系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。抽象输入动作，适配触摸、键鼠、手柄和重绑定。 玩法不应依赖具体设备。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。玩法不应依赖具体设备。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。玩法不应依赖具体设备。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「输入系统」匹配的真实案例：新 Input System。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- UI 和战斗抢输入——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "372": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 子弹和伤害数字。 中，我负责 对象池系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 对象池系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。支持预热、获取、回收、容量裁剪、状态重置和统计。 池化降低分配但增加常驻内存。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。池化降低分配但增加常驻内存。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。池化降低分配但增加常驻内存。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「对象池系统」匹配的真实案例：子弹和伤害数字。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 回收不清状态——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "373": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 错误上报带玩家 id。 中，我负责 日志系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 日志系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。日志分级、模块、采样、上下文和隐私脱敏。 日志为线上排障服务。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。日志为线上排障服务。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。日志为线上排障服务。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「日志系统」匹配的真实案例：错误上报带玩家 id。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 打印 token——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "374": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 新手漏斗。 中，我负责 埋点系统。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 埋点系统 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。统一事件名、字段、版本、缓存和批量上报。数据质量取决于规范。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。数据质量取决于规范。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。数据质量取决于规范。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「埋点系统」匹配的真实案例：新手漏斗。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 事件名随意——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "375": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Bugly 聚合。 中，我负责 崩溃监控。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 崩溃监控 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。上传符号表，收集日志、设备、版本、资源号和用户路径。 崩溃需要还原现场。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。崩溃需要还原现场。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。崩溃需要还原现场。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「崩溃监控」匹配的真实案例：Bugly 聚合。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 无符号无法定位——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "376": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Jenkins 打渠道包。 中，我负责 自动打包流程。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 自动打包流程 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。CI 拉代码、构建资源、打包、签名、上传和通知。 自动化减少人为错误。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。自动化减少人为错误。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。自动化减少人为错误。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「自动打包流程」匹配的真实案例：Jenkins 打渠道包。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 本机手动打包——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "377": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 提交前检查贴图。 中，我负责 资源检查工具。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 资源检查工具 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。检查命名、尺寸、格式、引用、重复依赖和非法目录。 资源问题应前置发现。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。资源问题应前置发现。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。资源问题应前置发现。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「资源检查工具」匹配的真实案例：提交前检查贴图。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只靠人工 Review——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "378": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 SVC 预热。 中，我负责 Shader 管理。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 Shader 管理 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。控制关键字、收集变体、预热、分档和平台测试。 Shader 影响包体和首帧。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。Shader 影响包体和首帧。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。Shader 影响包体和首帧。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「Shader 管理」匹配的真实案例：SVC 预热。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- multi_compile 滥用——过度优化或错误优化导致可读性下降且 Profile 无收益\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "379": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 低中高三档。 中，我负责 画质分级。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 画质分级 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。按设备性能配置分辨率、阴影、后处理、特效和帧率。 移动端差异大。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。移动端差异大。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。移动端差异大。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「画质分级」匹配的真实案例：低中高三档。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 所有设备同配置——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "380": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 战斗前预热。 中，我负责 加载优化。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 加载优化 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。拆 IO、解压、实例化、Shader 预热和初始化，按优先级分帧。 卡顿来源不只资源下载。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。卡顿来源不只资源下载。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。卡顿来源不只资源下载。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「加载优化」匹配的真实案例：战斗前预热。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 异步后一次实例化——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "381": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 切场景内存回落。 中，我负责 内存优化。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 内存优化 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。用 Memory Profiler 对比快照，查大资源、泄漏和常驻缓存。 内存问题看引用链。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。内存问题看引用链。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。内存问题看引用链。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「内存优化」匹配的真实案例：切场景内存回落。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只调用 GC.Collect——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "382": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 UI Overdraw 优化。 中，我负责 GPU 优化。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 GPU 优化 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。看 Frame Debugger 和 GPU Profiler，优化透明、阴影、后处理和分辨率。 移动 GPU 常受像素和带宽限制。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。移动 GPU 常受像素和带宽限制。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。移动 GPU 常受像素和带宽限制。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「GPU 优化」匹配的真实案例：UI Overdraw 优化。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只看 DrawCall——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "383": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Canvas Rebuild 降低。 中，我负责 CPU 优化。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 CPU 优化 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。定位脚本、物理、动画、UI 和资源加载热点，必要时 Job/Burst。 CPU 卡顿常在主线程。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。CPU 卡顿常在主线程。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。CPU 卡顿常在主线程。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「CPU 优化」匹配的真实案例：Canvas Rebuild 降低。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 盲目多线程——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "384": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 登录重试。 中，我负责 弱网优化。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 弱网优化 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。超时、重试退避、断线重连、请求缓存和 UI 状态恢复。移动网络不稳定是常态。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。移动网络不稳定是常态。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。移动网络不稳定是常态。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「弱网优化」匹配的真实案例：登录重试。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 失败直接踢出——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "385": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 战斗结算校验。 中，我负责 反作弊方案。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 反作弊方案 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。核心数值服务器权威，客户端只表现和提交可验证过程。 客户端不可信。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。客户端不可信。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。客户端不可信。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「反作弊方案」匹配的真实案例：战斗结算校验。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 信任客户端胜利——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "386": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Roslyn 分析器。 中，我负责 团队代码规范。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 团队代码规范 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。命名、生命周期、事件、异步、资源释放和异常处理规范工具化。 规范降低协作成本。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。规范降低协作成本。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。规范降低协作成本。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「团队代码规范」匹配的真实案例：Roslyn 分析器。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只有文档无检查——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "387": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 PR 模板。 中，我负责 Code Review 流程。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 Code Review 流程 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。小 PR、明确范围、检查正确性、性能、异常和测试。 Review 是质量和知识机制。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。Review 是质量和知识机制。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。Review 是质量和知识机制。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「Code Review 流程」匹配的真实案例：PR 模板。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只看格式——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "388": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 战斗公式 NUnit。 中，我负责 测试策略。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 测试策略 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。纯逻辑单测，关键流程集成测试，构建和资源校验进 CI。 测试分层覆盖风险。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。测试分层覆盖风险。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。测试分层覆盖风险。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「测试策略」匹配的真实案例：战斗公式 NUnit。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只测手动流程——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "389": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 Android/iOS/微信。 中，我负责 跨平台适配。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 跨平台适配 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。把登录、支付、分享、权限、存储抽象成平台服务。 平台差异不应散落业务。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。平台差异不应散落业务。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。平台差异不应散落业务。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「跨平台适配」匹配的真实案例：Android/iOS/微信。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 到处写宏——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "390": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 推资源检查流程。 中，我负责 团队协作中的技术推动。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 团队协作中的技术推动 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。用问题数据、方案对比、试点和文档推动，而不是只讲理念。 技术方案需要组织落地。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。技术方案需要组织落地。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。技术方案需要组织落地。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「团队协作中的技术推动」匹配的真实案例：推资源检查流程。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 强推无共识——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "391": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 热更框架选型回滚。 中，我负责 一次失败的技术选型。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 一次失败的技术选型 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。讲清当时约束、错误判断、影响、修正和经验。 面试看反思能力。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。面试看反思能力。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。面试看反思能力。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「一次失败的技术选型」匹配的真实案例：热更框架选型回滚。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 甩锅别人——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "392": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 排期技术评审。 中，我负责 主程的日常职责。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 主程的日常职责 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。技术方案、难点攻关、Code Review、风险识别、工具链和人员培养。 主程关注系统和团队效率。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。主程关注系统和团队效率。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。主程关注系统和团队效率。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「主程的日常职责」匹配的真实案例：排期技术评审。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只写最难代码——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "393": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 首包减少 40%。 中，我负责 你最大的技术亮点。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 你最大的技术亮点 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。选一个有权责、有难度、有数据结果的案例，用 STAR 结构说明，突出你的决策与贡献。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。亮点要有对比和收益。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。亮点要有对比和收益。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「你最大的技术亮点」匹配的真实案例：首包减少 40%。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 讲得太泛——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  },
  "394": {
    "answer": "- **S（情境）**：交代项目背景——类型（RPG/MMO/小游戏）、平台、团队规模、当时约束（上线节点、包体/内存上限、弱网）。例：「在 深入性能和工程化。 中，我负责 未来技术成长方向。」\n- **T（任务）**：明确个人职责与可量化目标，避免只说「我们做了」。例：「我需要在 X 周内把 未来技术成长方向 的 XX 指标从 A 改善到 B，并保证策划可配置。」\n- **A（行动）**：讲技术决策链——方案 A/B 对比、为何选当前方案、关键类/API、难点与取舍。结合岗位方向，说明 1~2 个可执行的学习计划（如渲染、性能、架构、工具链或 AI 工程化），并给出近期行动项与验证方式，体现持续成长意识。\n- **R（结果）**：用数据收尾（帧率、内存、崩溃率、开发效率、线上指标），并复盘「重做会改什么」。面试看长期潜力。\n- **Concrete talking points**：准备 2~3 个可被深挖的细节（类名如 PlayerSpawnPoint、一次线上事故、Profile 对比截图），证明你真正做过而非背题。",
    "principle": "项目深挖题考察表达结构与决策深度，而非再背知识点。面试看长期潜力。 面试官通过追问类名、时序、数据验证 STAR 真实性；",
    "project": "准备与「未来技术成长方向」匹配的真实案例：深入性能和工程化。。提前写清你的 Task 边界，Action 列 3 个技术关键词（如 Addressables/对象池/状态机），Result 准备 2 个数字。",
    "mistakes": "- 只说想当主程——导致运行时异常或性能回退\n- 只说「我们团队」不说「我负责」——贡献模糊，易被判定为旁听。\n- Result 无数据、无复盘——显得夸大或缺乏工程严谨性。"
  }
}